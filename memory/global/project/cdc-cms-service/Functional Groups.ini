NOTE:
### I. Danh Sách Các Nhóm Chức Năng (Functional Groups)

Cấu Trúc Các Nhóm Chức Năng
1. Nhóm source (Nguồn & Thu thập) : Chịu trách nhiệm toàn bộ vòng đời từ lúc khai báo database gốc đến lúc cắm ống hút dữ liệu.
Thành phần: Source Objects, Table Registry, Schema Scan, Debezium Connectors.

2. Nhóm shadow (Vùng đệm & Luân chuyển) : Quản lý trạng thái trung gian của dữ liệu sau khi rời khỏi Nguồn và trước khi đáp xuống Đích.
Thành phần: Shadow Bindings, Schema Transform.

3. Nhóm master (Quản lý Đích) : Chỉ tập trung vào việc dữ liệu sẽ được lưu trữ và ánh xạ như thế nào tại kho đích.
Thành phần: Master Registry, Master Bindings, Master Mapping Rules, Master Swaps.

4. Nhóm governance (Quản trị & Phê duyệt) : Tách riêng biệt để xử lý logic luồng công việc phức tạp, không làm phình to code của nhóm master.
Thành phần: Schema Proposals, Approval Workflows.

5. Nhóm recon (Đối soát & Sửa lỗi) : Đảm bảo tính toàn vẹn của dữ liệu cuối cùng.
Thành phần: Recon Check, Heal Data, Recon Reports, Retry Failed Logs, Backfill.

6. Nhóm orchestration (Điều phối lịch trình) : Đóng vai trò "nhạc trưởng" quản lý các tiến trình nền.
Thành phần: Async Jobs, Transmute Schedule, Worker Schedule, Wizard Sessions.

7. Nhóm system (Hệ thống & Giám sát) : 
Thành phần: Health Check, Metric/Probes, Alerts Manager, Audit Logs, Activity Logs.
---

### II. Kế Hoạch Thực Hiện (Refactoring Plan)

Với mỗi file Handler cũ được đưa vào, quy trình đập đi xây lại sẽ tuân thủ nghiêm ngặt theo luồng **Từ Trong Ra Ngoài (Inside-Out)** của Hexagonal:

* **Bước 1: Mapping & Routing (Nhận diện)** Xác định file thuộc Nhóm chức năng nào ở trên.
* **Bước 2: Define Ports (Tầng Domain)** Rút trích các thao tác DB/NATS thành các interface method đặt tại `internal/domain/<nhóm>/repository.go`.
* **Bước 3: Implement Use Cases (Tầng Application)** Đẩy các logic điều phối (if/else, vòng lặp, trigger) vào `internal/app/commands/<nhóm>/` hoặc `internal/app/queries/<nhóm>/`.
* **Bước 4: Implement Adapters (Tầng Infra)** Di chuyển toàn bộ GORM raw SQL và NATS logic vào `internal/infra/persistence/` và `internal/infra/messaging/`.
* **Bước 5: Slim down API (Tầng Delivery)** Gọt dũa file ở `internal/api/` chỉ còn nhận request HTTP, gọi Command/Query và trả JSON.

---

### III. BỘ PROMPT TIÊU CHUẨN (The Master Prompt)

*Anh hãy copy toàn bộ đoạn trong khung bên dưới. Mỗi khi cần cấu trúc lại một file, anh chỉ việc dán Prompt này kèm theo mã nguồn của file đó vào công cụ AI.*

---

**Ngữ cảnh (Context):**
Tôi đang tái cấu trúc một dự án Golang High-Throughput (CDC System) theo chuẩn **Hexagonal Architecture, CQRS, và Screaming Architecture**.
Hiện tại, tầng `internal/api/` đang chứa các "Fat Handlers" vi phạm nguyên tắc (nhúng trực tiếp GORM raw SQL, NATS client, và logic Use Case ngay trong HTTP Handler).

**Nhiệm vụ của bạn:**
Tôi sẽ cung cấp cho bạn mã nguồn của MỘT file ở tầng `internal/api/`. Bạn hãy đóng vai trò là một Senior Software/Data Architect và phân tích file này theo đúng 3 yêu cầu sau (trình bày rõ ràng, dùng bullet points):

#### 1. Nhận Diện Nhóm Chức Năng (Screaming Architecture)

* Xác định file này thuộc nhóm module nào trong hệ thống (Ví dụ: `master`, `source`, `shadow`, `recon`, `scheduler`, `system`).
* Liệt kê ngắn gọn các Use Case (hành vi nghiệp vụ) chính mà file này đang thực hiện (Ví dụ: Create Rule, Update Config, Sync Data...).

#### 2. Kế Hoạch Bóc Tách File (Chi Tiết Đường Dẫn & Chức Năng)

Hãy chẻ file tôi cung cấp ra thành các file nhỏ hơn theo đúng chuẩn. Ghi rõ **Đường dẫn file (Path)** và **Chức năng chi tiết (Functionality)** của từng file sẽ được tạo ra:

* **Tầng Domain (`internal/domain/<nhóm>/...`):** Cần bổ sung các method gì vào file Interface Repository/Publisher nào? (Lưu ý: Tuyệt đối mù công nghệ).
* **Tầng Application (`internal/app/commands/<nhóm>/...` hoặc `internal/app/queries/<nhóm>/...`):** Chẻ ra thành bao nhiêu file Command/Query? File nào sẽ xử lý Transaction? File nào sẽ điều phối NATS?
* **Tầng Infrastructure (`internal/infra/persistence/...` hoặc `messaging/...`):** File GORM nào sẽ chứa các đoạn raw SQL/Builder vừa được bóc ra?
* **Tầng API (`internal/api/<nhóm>_handler.go`):** File Handler mới sẽ giữ lại cấu trúc HTTP/Fiber mỏng nhẹ như thế nào?

#### 3. Phân Tích Khả Năng Tái Sử Dụng (Reusability Check)

Trước khi đề xuất tạo file mới, hãy kiểm tra:

* Có file Command/Query nào bị trùng lặp với các Core Pattern cơ bản không? (Nếu chỉ là CRUD đơn giản, có thể dùng chung thay vì tạo file mới).
* Có các hàm tiện ích/kiểm tra nào (như validate tên cột, check regex) nên được đưa ra thư mục dùng chung `pkgs/utils/` thay vì để kẹt trong Domain/App không?

**Mã nguồn file API cần phân tích:**


package api

import (
	"context"
	"encoding/json"
	"fmt"
	"strconv"
	"strings"
	"time"

	"cdc-cms-service/internal/domain/mapping"
	"cdc-cms-service/pkgs/natsconn"

	"github.com/gofiber/fiber/v2"
	"gorm.io/gorm"
)

// MasterMappingRuleHandler — master mapping rules link to mapping_rule_v2 (business
// rules of the shadow) via mapping_v2_id. NO shadow DB access here (master must not
// touch shadow data plane); flatten/scan-array belongs to the shadow mapping layer.
type MasterMappingRuleHandler struct {
	db         *gorm.DB
	repo       mapping.MasterRuleRepository
	natsClient *natsconn.NatsClient
}

func NewMasterMappingRuleHandler(db *gorm.DB, repo mapping.MasterRuleRepository, natsClient *natsconn.NatsClient) *MasterMappingRuleHandler {
	return &MasterMappingRuleHandler{db: db, repo: repo, natsClient: natsClient}
}

// List handles GET /api/v1/master-mapping-rules?master_binding_id=X
// Returns master rules JOINed with mapping_rule_v2 (source_field/data_type come from v2).
func (h *MasterMappingRuleHandler) List(c *fiber.Ctx) error {
	masterBindingID, _ := strconv.ParseInt(c.Query("master_binding_id"), 10, 64)
	if masterBindingID == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "master_binding_id is required"})
	}

	filter := mapping.MasterFilter{
		MasterBindingID: masterBindingID,
		Status:          mapping.Status(c.Query("status")),
	}
	if active := c.Query("is_active"); active != "" {
		isActive := active == "true"
		filter.IsActive = &isActive
	}

	rules, err := h.repo.List(c.Context(), filter)
	if err != nil {
		return c.Status(500).JSON(fiber.Map{"error": "failed to list master rules: " + err.Error()})
	}
	return c.JSON(fiber.Map{"data": rules, "count": len(rules)})
}

type CreateOrUpdateMasterRuleRequest struct {
	ID              int64   `json:"id"`
	MasterBindingID int64   `json:"master_binding_id"`
	MappingV2ID     int64   `json:"mapping_v2_id"`
	TargetColumn    string  `json:"target_column"`
	IsActive        *bool   `json:"is_active"`
	Status          string  `json:"status"`
	Notes           *string `json:"notes"`
	SourceField     string  `json:"source_field"`
	SourcePath      *string `json:"source_path"`
	DataType        string  `json:"data_type"`
}

// Save handles POST /api/v1/master-mapping-rules (create/update upsert by mapping_v2_id)
func (h *MasterMappingRuleHandler) Save(c *fiber.Ctx) error {
	var req CreateOrUpdateMasterRuleRequest
	if err := c.BodyParser(&req); err != nil {
		return c.Status(400).JSON(fiber.Map{"error": "invalid request body"})
	}
	if req.MasterBindingID == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "master_binding_id is required"})
	}
	selfContained := req.MappingV2ID == 0
	if selfContained && (req.SourcePath == nil || strings.TrimSpace(*req.SourcePath) == "") {
		return c.Status(400).JSON(fiber.Map{"error": "cần mapping_v2_id (link shadow field) HOẶC source_path (field nested self-contained)"})
	}
	if req.TargetColumn == "" {
		return c.Status(400).JSON(fiber.Map{"error": "target_column is required"})
	}
	if isSystemColumn(req.TargetColumn) {
		return c.Status(400).JSON(fiber.Map{"error": "target_column cannot be a system column: " + req.TargetColumn})
	}

	status := req.Status
	if status == "" {
		status = "pending"
	}
	isActive := true
	if req.IsActive != nil {
		isActive = *req.IsActive
	}
	actor, _ := c.Locals("username").(string)
	if actor == "" {
		actor = "api"
	}

	rule := &mapping.MasterRule{
		ID:              req.ID,
		MasterBindingID: req.MasterBindingID,
		MappingV2ID:     req.MappingV2ID,
		TargetColumn:    req.TargetColumn,
		IsActive:        isActive,
		Status:          mapping.Status(status),
		Notes:           req.Notes,
		CreatedBy:       &actor,
		UpdatedBy:       &actor,
		SourceField:     req.SourceField,
		SourcePath:      req.SourcePath,
		DataType:        req.DataType,
	}
	if err := h.repo.Save(c.Context(), rule); err != nil {
		return c.Status(500).JSON(fiber.Map{"error": "failed to save master rule: " + err.Error()})
	}
	if rule.Status == "approved" {
		username, _ := c.Locals("username").(string)
		if username == "" {
			username = "api-save"
		}
		if err := h.triggerMasterDDL(c.Context(), rule.MasterBindingID, username); err != nil {
			fmt.Printf("[CRITICAL] Saved rule %d but failed to trigger DDL on NATS: %v\n", rule.ID, err)
		}
	}
	return c.JSON(fiber.Map{"status": "saved", "rule": rule})
}

// Delete handles DELETE /api/v1/master-mapping-rules/:id
func (h *MasterMappingRuleHandler) Delete(c *fiber.Ctx) error {
	id, _ := strconv.ParseInt(c.Params("id"), 10, 64)
	if id == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "id parameter required"})
	}
	existing, err := h.repo.GetByID(c.Context(), id)
	if err != nil {
		return c.Status(404).JSON(fiber.Map{"error": "master rule not found"})
	}
	if existing.CreatedBy != nil && *existing.CreatedBy == "shadow-sync" {
		return c.Status(403).JSON(fiber.Map{"error": "field này sync từ shadow, không được xoá (sửa/xoá ở tầng shadow)"})
	}
	if err := h.repo.Delete(c.Context(), id); err != nil {
		return c.Status(500).JSON(fiber.Map{"error": "failed to delete master rule: " + err.Error()})
	}
	return c.JSON(fiber.Map{"status": "deleted"})
}

// UpdateTargetColumn handles PUT /api/v1/master-mapping-rules/:id/target-column
func (h *MasterMappingRuleHandler) UpdateTargetColumn(c *fiber.Ctx) error {
	id, _ := strconv.ParseInt(c.Params("id"), 10, 64)
	if id == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "id parameter required"})
	}
	var req struct {
		TargetColumn string `json:"target_column"`
	}
	if err := c.BodyParser(&req); err != nil {
		return c.Status(400).JSON(fiber.Map{"error": "invalid body"})
	}
	req.TargetColumn = strings.TrimSpace(req.TargetColumn)
	if !isValidColumnName(req.TargetColumn) {
		return c.Status(400).JSON(fiber.Map{"error": "target_column không hợp lệ (chỉ chữ/số/_ , bắt đầu bằng chữ hoặc _, ≤63 ký tự)"})
	}
	if isSystemColumn(req.TargetColumn) {
		return c.Status(400).JSON(fiber.Map{"error": "không được phép đổi tên thành cột hệ thống"})
	}

	existing, gerr := h.repo.GetByID(c.Context(), id)
	if gerr != nil {
		return c.Status(404).JSON(fiber.Map{"error": "master rule not found"})
	}

	var conflictID int64
	h.db.WithContext(c.Context()).Raw(`
        SELECT id FROM cdc_system.mapping_rule_master 
         WHERE master_binding_id = ? AND target_column = ? AND id <> ? LIMIT 1`,
		existing.MasterBindingID, req.TargetColumn, id).Scan(&conflictID)
	if conflictID > 0 {
		return c.Status(409).JSON(fiber.Map{"error": "target_column này đã được sử dụng bởi một rule khác trong bảng này"})
	}

	oldCol := existing.TargetColumn
	stageRename := existing.InMaster && oldCol != req.TargetColumn
	var err error
	if stageRename {
		err = h.db.WithContext(c.Context()).Exec(
			`UPDATE cdc_system.mapping_rule_master
			    SET target_column = ?, pending_rename_from = NULLIF(COALESCE(pending_rename_from, ?), ?), updated_at = NOW()
			  WHERE id = ?`,
			req.TargetColumn, oldCol, req.TargetColumn, id,
		).Error
	} else {
		err = h.db.WithContext(c.Context()).Exec(
			`UPDATE cdc_system.mapping_rule_master SET target_column = ?, updated_at = NOW() WHERE id = ?`,
			req.TargetColumn, id,
		).Error
	}
	if err != nil {
		return c.Status(500).JSON(fiber.Map{"error": "update failed: " + err.Error()})
	}
	return c.JSON(fiber.Map{"status": "updated", "target_column": req.TargetColumn, "ddl_pending": stageRename})
}

// UpdateDataType handles PUT /api/v1/master-mapping-rules/:id/data-type
func (h *MasterMappingRuleHandler) UpdateDataType(c *fiber.Ctx) error {
	id, _ := strconv.ParseInt(c.Params("id"), 10, 64)
	if id == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "id parameter required"})
	}
	var req struct {
		DataType string `json:"data_type"`
	}
	if err := c.BodyParser(&req); err != nil {
		return c.Status(400).JSON(fiber.Map{"error": "invalid body"})
	}
	dt := strings.ToUpper(strings.TrimSpace(req.DataType))
	if !isValidDataType(dt) {
		return c.Status(400).JSON(fiber.Map{"error": "data_type không hợp lệ (chỉ VARCHAR/CHAR/TEXT/INTEGER/BIGINT/NUMERIC/BOOLEAN/TIMESTAMP/JSONB...)"})
	}
	existing, gerr := h.repo.GetByID(c.Context(), id)
	if gerr != nil {
		return c.Status(404).JSON(fiber.Map{"error": "master rule not found"})
	}

	stageType := existing.InMaster && existing.TargetColumn != ""
	var uerr error
	if stageType {
		uerr = h.db.WithContext(c.Context()).Exec(
			`UPDATE cdc_system.mapping_rule_master SET data_type = ?, pending_type_change = TRUE, updated_at = NOW() WHERE id = ?`,
			dt, id,
		).Error
	} else {
		uerr = h.db.WithContext(c.Context()).Exec(
			`UPDATE cdc_system.mapping_rule_master SET data_type = ?, updated_at = NOW() WHERE id = ?`,
			dt, id,
		).Error
	}
	if uerr != nil {
		return c.Status(500).JSON(fiber.Map{"error": "update failed: " + uerr.Error()})
	}
	return c.JSON(fiber.Map{"status": "updated", "data_type": dt, "ddl_pending": stageType})
}

// publishMasterReconcile gửi lệnh reconcile cột master DEST (rename + alter_type) tới
// worker VÀ chờ reply (sync, 60s) — caller clear pending CHỈ khi worker báo ok. DDL
// thật do worker chạy (control-plane CMS không có connection tới dest).
func (h *MasterMappingRuleHandler) publishMasterReconcile(ctx context.Context, masterTable, renameFrom, targetColumn, dataType string) error {
	if h.natsClient == nil || h.natsClient.Conn == nil {
		return fmt.Errorf("nats not available")
	}
	replySubj := fmt.Sprintf("cdc.evt.master-alter.%d", time.Now().UnixNano())
	sub, err := h.natsClient.Conn.SubscribeSync(replySubj)
	if err != nil {
		return err
	}
	defer sub.Unsubscribe()
	payload, _ := json.Marshal(map[string]any{
		"master_table":  masterTable,
		"rename_from":   renameFrom,
		"target_column": targetColumn,
		"data_type":     dataType,
		"reply_to":      replySubj,
	})
	if e := h.natsClient.Conn.Publish("cdc.cmd.master-alter-column", payload); e != nil {
		return e
	}
	msg, e := sub.NextMsg(60 * time.Second)
	if e != nil {
		return fmt.Errorf("worker timeout (reconcile %s)", targetColumn)
	}
	var res struct {
		Status string `json:"status"`
		Error  string `json:"error"`
	}
	_ = json.Unmarshal(msg.Data, &res)
	if res.Status != "ok" {
		if res.Error != "" {
			return fmt.Errorf("%s", res.Error)
		}
		return fmt.Errorf("reconcile failed")
	}
	return nil
}

// publishMasterDropColumn — gửi lệnh DROP cột master DEST tới worker + chờ reply (60s).
func (h *MasterMappingRuleHandler) publishMasterDropColumn(ctx context.Context, masterTable, targetColumn string) error {
	if h.natsClient == nil || h.natsClient.Conn == nil {
		return fmt.Errorf("nats not available")
	}
	replySubj := fmt.Sprintf("cdc.evt.master-drop.%d", time.Now().UnixNano())
	sub, err := h.natsClient.Conn.SubscribeSync(replySubj)
	if err != nil {
		return err
	}
	defer sub.Unsubscribe()
	payload, _ := json.Marshal(map[string]any{
		"master_table":  masterTable,
		"target_column": targetColumn,
		"action":        "drop",
		"reply_to":      replySubj,
	})
	if e := h.natsClient.Conn.Publish("cdc.cmd.master-alter-column", payload); e != nil {
		return e
	}
	msg, e := sub.NextMsg(60 * time.Second)
	if e != nil {
		return fmt.Errorf("worker timeout (drop %s)", targetColumn)
	}
	var res struct {
		Status string `json:"status"`
		Error  string `json:"error"`
	}
	_ = json.Unmarshal(msg.Data, &res)
	if res.Status != "ok" {
		if res.Error != "" {
			return fmt.Errorf("%s", res.Error)
		}
		return fmt.Errorf("drop failed")
	}
	return nil
}

// DropColumn — POST /api/v1/master-mapping-rules/:id/drop-column (destructive).
func (h *MasterMappingRuleHandler) DropColumn(c *fiber.Ctx) error {
	id, _ := c.ParamsInt("id")
	if id <= 0 {
		return c.Status(400).JSON(fiber.Map{"error": "invalid_id"})
	}
	var row struct {
		TargetColumn    string
		Status          string
		MasterBindingID int64
		MasterTable     string
	}
	if e := h.db.WithContext(c.Context()).Raw(`
		SELECT mr.target_column AS target_column, mr.status AS status,
		       mr.master_binding_id AS master_binding_id, mb.master_table AS master_table
		  FROM cdc_system.mapping_rule_master mr
		  JOIN cdc_system.master_binding mb ON mb.id = mr.master_binding_id
		 WHERE mr.id = ? LIMIT 1`, id).Scan(&row).Error; e != nil || row.TargetColumn == "" {
		return c.Status(404).JSON(fiber.Map{"error": "rule_not_found"})
	}
	if row.Status != "rejected" {
		return c.Status(409).JSON(fiber.Map{"error": "chỉ drop được cột của rule status='rejected' (rule này: " + row.Status + ")"})
	}
	if strings.HasPrefix(row.TargetColumn, "_") {
		return c.Status(400).JSON(fiber.Map{"error": "không drop cột hệ thống _*"})
	}
	var inUse int64
	h.db.WithContext(c.Context()).Raw(`
		SELECT count(*) FROM cdc_system.mapping_rule_master
		 WHERE master_binding_id = ? AND target_column = ? AND status = 'approved' AND is_active = true`,
		row.MasterBindingID, row.TargetColumn).Scan(&inUse)
	if inUse > 0 {
		return c.Status(409).JSON(fiber.Map{"error": "cột đang được rule approved+active dùng — không drop"})
	}
	if e := h.publishMasterDropColumn(c.Context(), row.MasterTable, row.TargetColumn); e != nil {
		return c.Status(502).JSON(fiber.Map{"error": "drop failed: " + e.Error()})
	}
	_ = h.db.WithContext(c.Context()).Exec(
		`UPDATE cdc_system.mapping_rule_master SET in_master = false, updated_at = NOW() WHERE master_binding_id = ? AND target_column = ?`,
		row.MasterBindingID, row.TargetColumn,
	).Error
	return c.JSON(fiber.Map{"status": "dropped", "id": id, "column": row.TargetColumn, "master_table": row.MasterTable})
}

// DropRejectedColumns — POST /api/v1/master-mapping-rules/drop-rejected-columns?binding_id=X
// (destructive). Drop cột orphan của TẤT CẢ rule rejected trong binding (loại cột _*
// và cột đang được rule approved+active dùng). Trả {dropped, failed, errors}.
func (h *MasterMappingRuleHandler) DropRejectedColumns(c *fiber.Ctx) error {
	bindingID, _ := strconv.ParseInt(c.Query("binding_id"), 10, 64)
	if bindingID <= 0 {
		return c.Status(400).JSON(fiber.Map{"error": "binding_id required"})
	}
	var masterTable string
	if e := h.db.WithContext(c.Context()).Raw(
		`SELECT master_table FROM cdc_system.master_binding WHERE id = ? LIMIT 1`, bindingID,
	).Scan(&masterTable).Error; e != nil || masterTable == "" {
		return c.Status(404).JSON(fiber.Map{"error": "binding_not_found"})
	}
	var cols []string
	h.db.WithContext(c.Context()).Raw(`
		SELECT DISTINCT mr.target_column FROM cdc_system.mapping_rule_master mr
		 WHERE mr.master_binding_id = ? AND mr.status = 'rejected'
		   AND mr.target_column NOT LIKE '\_%'
		   AND NOT EXISTS (
		       SELECT 1 FROM cdc_system.mapping_rule_master a
		        WHERE a.master_binding_id = mr.master_binding_id
		          AND a.target_column = mr.target_column
		          AND a.status = 'approved' AND a.is_active = true)`,
		bindingID).Scan(&cols)
	dropped, failed := 0, 0
	errs := []string{}
	for _, col := range cols {
		if e := h.publishMasterDropColumn(c.Context(), masterTable, col); e != nil {
			failed++
			errs = append(errs, col+": "+e.Error())
		} else {
			dropped++
			_ = h.db.WithContext(c.Context()).Exec(
				`UPDATE cdc_system.mapping_rule_master SET in_master = false, updated_at = NOW() WHERE master_binding_id = ? AND target_column = ?`,
				bindingID, col,
			).Error
		}
	}
	return c.JSON(fiber.Map{"status": "done", "binding_id": bindingID, "candidates": len(cols), "dropped": dropped, "failed": failed, "errors": errs})
}

// applyPendingDDL apply pending DDL (rename + alter_type) của 1 rule lên dest qua worker;
// clear pending flag CHỈ khi worker ok. Trả (applied, err). applied=false nếu rule
// không có gì pending.
func (h *MasterMappingRuleHandler) applyPendingDDL(ctx context.Context, rule *mapping.MasterRule) (bool, error) {
	hasRename := rule.PendingRenameFrom != nil && *rule.PendingRenameFrom != "" && *rule.PendingRenameFrom != rule.TargetColumn
	if !hasRename && !rule.PendingTypeChange {
		return false, nil
	}
	var masterTable string
	if e := h.db.WithContext(ctx).Raw(
		`SELECT master_table FROM cdc_system.master_binding WHERE id = ? LIMIT 1`, rule.MasterBindingID,
	).Scan(&masterTable).Error; e != nil || masterTable == "" {
		return false, fmt.Errorf("master_table not found")
	}
	renameFrom := ""
	if hasRename {
		renameFrom = *rule.PendingRenameFrom
	}
	dataType := ""
	if rule.PendingTypeChange {
		dataType = rule.DataType
	}
	if e := h.publishMasterReconcile(ctx, masterTable, renameFrom, rule.TargetColumn, dataType); e != nil {
		return false, e
	}
	_ = h.db.WithContext(ctx).Exec(
		`UPDATE cdc_system.mapping_rule_master SET pending_rename_from = NULL, pending_type_change = FALSE, updated_at = NOW() WHERE id = ?`,
		rule.ID,
	).Error
	return true, nil
}

// ApproveDDL handles POST /api/v1/master-mapping-rules/:id/approve-ddl — apply pending
// DDL (rename/alter_type) của 1 rule lên bảng master dest.
func (h *MasterMappingRuleHandler) ApproveDDL(c *fiber.Ctx) error {
	id, _ := strconv.ParseInt(c.Params("id"), 10, 64)
	if id == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "id parameter required"})
	}
	rule, err := h.repo.GetByID(c.Context(), id)
	if err != nil {
		return c.Status(404).JSON(fiber.Map{"error": "master rule not found"})
	}
	applied, aerr := h.applyPendingDDL(c.Context(), rule)
	if aerr != nil {
		return c.Status(500).JSON(fiber.Map{"error": "apply DDL thất bại: " + aerr.Error()})
	}
	return c.JSON(fiber.Map{"status": "ok", "applied": applied})
}

// ApproveDDLAll handles POST /api/v1/master-mapping-rules/approve-ddl-all?master_binding_id=X
// — apply pending DDL cho TẤT CẢ rule có pending của 1 master binding.
func (h *MasterMappingRuleHandler) ApproveDDLAll(c *fiber.Ctx) error {
	masterBindingID, _ := strconv.ParseInt(c.Query("master_binding_id"), 10, 64)
	if masterBindingID == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "master_binding_id required"})
	}
	rules, err := h.repo.List(c.Context(), mapping.MasterFilter{MasterBindingID: masterBindingID})
	if err != nil {
		return c.Status(500).JSON(fiber.Map{"error": err.Error()})
	}
	applied, failed := 0, 0
	var errs []string
	for _, r := range rules {
		ok, e := h.applyPendingDDL(c.Context(), r)
		if e != nil {
			failed++
			errs = append(errs, fmt.Sprintf("%s: %s", r.TargetColumn, e.Error()))
			continue
		}
		if ok {
			applied++
		}
	}
	return c.JSON(fiber.Map{"status": "ok", "applied": applied, "failed": failed, "errors": errs})
}

// isValidDataType: whitelist kiểu Postgres (kèm length/precision tuỳ chọn) để chống
// injection qua data_type + đảm bảo DDL ALTER COLUMN sinh được.
func isValidDataType(s string) bool {
	s = strings.ToUpper(strings.TrimSpace(s))
	if s == "" || len(s) > 50 {
		return false
	}
	base := s
	if i := strings.IndexByte(s, '('); i >= 0 {
		if !strings.HasSuffix(s, ")") {
			return false
		}
		base = strings.TrimSpace(s[:i])
		inner := s[i+1 : len(s)-1]
		if inner == "" {
			return false
		}
		for _, r := range inner {
			if !((r >= '0' && r <= '9') || r == ',' || r == ' ') {
				return false
			}
		}
	}
	allowed := map[string]bool{
		"VARCHAR": true, "CHAR": true, "TEXT": true, "INTEGER": true, "INT": true,
		"BIGINT": true, "SMALLINT": true, "NUMERIC": true, "DECIMAL": true, "REAL": true,
		"DOUBLE PRECISION": true, "BOOLEAN": true, "BOOL": true, "DATE": true,
		"TIMESTAMP": true, "TIMESTAMPTZ": true, "JSONB": true, "JSON": true, "UUID": true,
	}
	return allowed[base]
}

// isValidColumnName: identifier Postgres an toàn (không cần quote) — chống injection
// qua tên cột + đảm bảo DDL sinh được.
func isValidColumnName(s string) bool {
	if s == "" || len(s) > 63 {
		return false
	}
	for i, r := range s {
		isLetter := (r >= 'a' && r <= 'z') || (r >= 'A' && r <= 'Z') || r == '_'
		isDigit := r >= '0' && r <= '9'
		if i == 0 && !isLetter {
			return false
		}
		if i > 0 && !isLetter && !isDigit {
			return false
		}
	}
	return true
}

// SyncFromShadow handles POST /api/v1/master-mapping-rules/sync-from-shadow
//
// Pull các business rule mới của shadow (mapping_rule_v2) vào mapping_rule_master
// cho master binding cụ thể. Chỉ thêm các rule chưa có (ON CONFLICT DO NOTHING).
// Rule mới được thêm với status='pending' để operator review trước khi duyệt.
//
// Lý do tồn tại: clone trong create_master chỉ chạy 1 lần lúc tạo. Khi shadow
// thêm field mới sau đó, cần gọi endpoint này để sync sang master.
func (h *MasterMappingRuleHandler) SyncFromShadow(c *fiber.Ctx) error {
	masterBindingID, _ := strconv.ParseInt(c.Query("master_binding_id"), 10, 64)
	if masterBindingID == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "master_binding_id is required"})
	}

	updatedBy := c.Query("updated_by")
	if updatedBy == "" {
		updatedBy = "sync-from-shadow"
	}

	// Lấy shadow_binding_id của master này để filter đúng source.
	var shadowBindingID int64
	if err := h.db.WithContext(c.Context()).Raw(
		`SELECT shadow_binding_id FROM cdc_system.master_binding WHERE id = ? LIMIT 1`,
		masterBindingID,
	).Scan(&shadowBindingID).Error; err != nil || shadowBindingID == 0 {
		return c.Status(404).JSON(fiber.Map{"error": "master binding not found or shadow_binding_id missing"})
	}

	updateRes := h.db.WithContext(c.Context()).Exec(`
        UPDATE cdc_system.mapping_rule_master m
           SET target_column = v2.target_column, updated_at = NOW(), updated_by = ?
          FROM cdc_system.mapping_rule_v2 v2
         WHERE m.mapping_v2_id = v2.id
           AND m.master_binding_id = ?
           AND m.target_column <> v2.target_column
           AND m.status = 'pending'
           AND v2.shadow_binding_id = ?
           AND v2.is_active = true
           AND lower(v2.target_column) NOT IN (
             '_gpay_id','_source_id','_raw_data','_source','_source_ts','_synced_at','_version','_hash','_deleted','_created_at','_updated_at')
    `, updatedBy, masterBindingID, shadowBindingID)
	if updateRes.Error != nil {
		return c.Status(500).JSON(fiber.Map{"error": "sync update phase failed: " + updateRes.Error.Error()})
	}

	insertRes := h.db.WithContext(c.Context()).Exec(`
        INSERT INTO cdc_system.mapping_rule_master
          (master_binding_id, mapping_v2_id, target_column, is_active, status, created_by, updated_by)
        SELECT ?, v2.id, v2.target_column, true, 'pending', ?, ?
          FROM cdc_system.mapping_rule_v2 v2
         WHERE v2.shadow_binding_id = ?
           AND NOT EXISTS (
             SELECT 1 FROM cdc_system.mapping_rule_master mm
              WHERE mm.master_binding_id = ? AND mm.mapping_v2_id = v2.id)
           AND v2.is_active = true
           AND lower(v2.target_column) NOT IN (
             '_gpay_id','_source_id','_raw_data','_source','_source_ts','_synced_at','_version','_hash','_deleted','_created_at','_updated_at')
        ON CONFLICT (master_binding_id, target_column) DO NOTHING`,
		masterBindingID, "shadow-sync", updatedBy, shadowBindingID, masterBindingID,
	)
	if insertRes.Error != nil {
		return c.Status(500).JSON(fiber.Map{"error": "sync insert phase failed: " + insertRes.Error.Error()})
	}

	return c.JSON(fiber.Map{
		"status":         "synced",
		"synced_new":     insertRes.RowsAffected,
		"synced_renamed": updateRes.RowsAffected,
	})
}

type BatchUpdateMasterRulesRequest struct {
	IDs       []int64 `json:"ids"`
	Status    string  `json:"status"` // 'approved', 'rejected'
	IsActive  *bool   `json:"is_active,omitempty"`
	UpdatedBy string  `json:"updated_by,omitempty"`
}

// BatchUpdate handles PUT /api/v1/master-mapping-rules/batch
func (h *MasterMappingRuleHandler) BatchUpdate(c *fiber.Ctx) error {
	var req BatchUpdateMasterRulesRequest
	if err := c.BodyParser(&req); err != nil {
		return c.Status(400).JSON(fiber.Map{"error": "invalid request body"})
	}
	if len(req.IDs) == 0 {
		return c.Status(400).JSON(fiber.Map{"error": "no rule ids provided"})
	}
	if len(req.IDs) > 1000 {
		return c.Status(400).JSON(fiber.Map{"error": "số lượng rule xử lý vượt giới hạn (max 1000)"})
	}
	var bindingIDs []int64
	if req.Status == "approved" {
		h.db.WithContext(c.Context()).Raw(
			`SELECT DISTINCT master_binding_id FROM cdc_system.mapping_rule_master WHERE id IN (?)`,
			req.IDs,
		).Scan(&bindingIDs)
	}

	txErr := h.db.WithContext(c.Context()).Transaction(func(tx *gorm.DB) error {
		updates := make(map[string]interface{})
		updates["updated_at"] = gorm.Expr("NOW()")
		if req.Status != "" {
			updates["status"] = req.Status
		}
		if req.IsActive != nil {
			updates["is_active"] = *req.IsActive
		}
		if req.UpdatedBy != "" {
			updates["updated_by"] = req.UpdatedBy
		}

		return tx.Table("cdc_system.mapping_rule_master").
			Where("id IN ?", req.IDs).
			Updates(updates).Error
	})

	if txErr != nil {
		return c.Status(500).JSON(fiber.Map{"error": "batch update failed: " + txErr.Error()})
	}

	if req.Status == "approved" && len(bindingIDs) > 0 {
		username := req.UpdatedBy
		if username == "" {
			username, _ = c.Locals("username").(string)
		}
		if username == "" {
			username = "api-batch"
		}

		var ddlErrs []string
		for _, bid := range bindingIDs {
			if err := h.triggerMasterDDL(c.Context(), bid, username); err != nil {
				ddlErrs = append(ddlErrs, fmt.Sprintf("binding %d: %v", bid, err))
			}
		}

		if len(ddlErrs) > 0 {
			fmt.Printf("[CRITICAL] Batch update saved but DDL trigger failed for: %v\n", ddlErrs)
			return c.Status(207).JSON(fiber.Map{
				"status":  "partial_success",
				"count":   len(req.IDs),
				"warning": "Rules đã duyệt nhưng Worker không nhận được lệnh DDL",
			})
		}
	}

	return c.JSON(fiber.Map{"status": "updated", "count": len(req.IDs)})
}

func (h *MasterMappingRuleHandler) triggerMasterDDL(ctx context.Context, masterBindingID int64, triggeredBy string) error {
	if h.natsClient == nil || h.natsClient.Conn == nil {
		return fmt.Errorf("nats client nil")
	}
	var masterTable string
	if err := h.db.WithContext(ctx).Raw(
		`SELECT master_table FROM cdc_system.master_binding WHERE id = ? LIMIT 1`,
		masterBindingID,
	).Scan(&masterTable).Error; err != nil || masterTable == "" {
		return fmt.Errorf("master_table not found")
	}

	payload, _ := json.Marshal(map[string]string{
		"master_table":   masterTable,
		"triggered_by":   triggeredBy,
		"correlation_id": "rule-approve-" + masterTable + "-" + time.Now().UTC().Format(time.RFC3339Nano),
	})

	if err := h.natsClient.Conn.Publish("cdc.cmd.master-create", payload); err != nil {
		return err
	}
	return nil
}

// isSystemColumn guards target_column against shadow INFRASTRUCTURE column leakage.
// Chỉ block các cột do sinkworker tự tạo (prefix _ của hạ tầng CDC).
// KHÔNG block business field như status/progress/error/params dù trùng tên.
func isSystemColumn(col string) bool {
	blacklist := []string{
		"_gpay_id", "_source_id", "_raw_data", "_source", "_source_ts", "_synced_at", "_version", "_hash", "_deleted", "_created_at", "_updated_at",
	}
	colLower := strings.ToLower(strings.TrimSpace(col))
	for _, b := range blacklist {
		if colLower == b {
			return true
		}
	}
	return false
}

// (G1) MasterColumns endpoint đã gỡ: nó query information_schema trên control-plane
// (h.db = cdc_dw 5433) trong khi master table vật lý ở dest (5434) → luôn rỗng. "In
// Master" giờ dùng flag worker-maintained `mapping_rule_master.in_master` (worker set
// khi Apply DDL — chỉ worker chạm dest). FE đọc flag qua GET List, không cần endpoint này.





Note : 
- Đọc lesson trước tất cả.
- Làm theo core /agent, nhớ đọc file  "work/agent/GEMINI.md" để hiểu role, skill khi làm.
- Xác nhận đã đọc "work/agent/GEMINI.md" chưa. 
- "Simplicity First, minimal impact".     
- Luôn làm đúng những gì đc yêu cầu, theo đúng patten của source, và tối ưu nhất. 
- Luôn làm theo hướng core systems, không cheat db hay thay đổi các config để đạt đc kêt quả.
- Phải luôn có plan rõ ràng, plan phải rõ ràng, có giải pháp cụ thể (có code demo tới từng chi tiết)
- Khi report phải dựa trên kết quả tính toán thực tế, review cẩn thận, có note lại các file thay đổi, vì sao ko đc report láo. 
- Khi kết thúc luôn kiểm tra các service work mới báo done.
- Luôn có 1 file report_*.md ghi lại những gì thay đổi, "những file đã thay đổi", "số lượng dòng code" đã thay đổi để xem lại.
- Luôn đề xuất hướng tốt nhất, đừng có cái giọng điệu option 1,2,3

Quy tắc : 
1. Tầng internal/domain: chứa các interface method đặt tại `internal/domain/<nhóm>/repository.go`
2. Tầng internal/app: chứa các logic điều phối internal/app/commands/<nhóm>/` hoặc `internal/app/queries/<nhóm>/`.
3. Tầng internal/infra: chứa toàn bộ GORM và NATS logic `internal/infra/persistence/<nhóm>/` và `internal/infra/messaging/<nhóm>/`.
4. Tầng internal/api: Gọt dũa file ở `internal/api/<nhóm>` chỉ còn nhận request HTTP, gọi Command/Query và trả JSON.

Task:
1. Hiện tại ở internal/api, internal/app vẫn tồn tại h.db và client nats => di chuyển về internal/infra
2. Rà soát các file ở internal/app, internal/api, internal/infra xem còn sót logic nào cần xử lý lại và di chuyển tiếp không
3. Audit các câu sql và nats ở tầng infra xem có sai sót so với /work/data-hub-bf/cdc-cms-service/ không


Với tất cả các api & folow của nó, hãy đi check toàn bộ từng dòng logic, đừng có bỏ sót.
review và xuất file full, đảm bảo đủ logic, ko cắt xén