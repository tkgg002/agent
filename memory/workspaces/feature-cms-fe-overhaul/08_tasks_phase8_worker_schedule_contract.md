# Tasks — Phase 8 Worker Schedule Contract

1. Cập nhật workspace context với mô hình 2 luồng.
2. Audit API `worker-schedule` và metadata V2.
3. Refactor contract backend `worker-schedule`.
4. Refactor `ActivityManager` theo contract mới.
5. Update swagger/comment cho endpoint bị đổi.
6. Chạy verify backend + frontend.
