# Tasks — Phase 17 cdc_system Namespace Bridge

1. Audit CMS runtime models/repos còn trỏ `cdc_internal`.
2. Đối chiếu với migrations end-state.
3. Sửa namespace runtime đang dùng thật.
4. Verify grep + backend tests.
