# Tasks — Phase 9 Master Binding Contract

1. Audit API `v1/masters` và deep link từ `TableRegistry`.
2. Refactor backend `master_registry_handler.go` sang V2 metadata.
3. Update swagger/comment cho `v1/masters`.
4. Refactor `MasterRegistry.tsx` và `TableRegistry.tsx`.
5. Verify backend test + FE build.
