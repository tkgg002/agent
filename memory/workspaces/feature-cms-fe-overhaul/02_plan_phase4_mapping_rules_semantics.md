# Plan — Phase 4 Mapping Rules Semantics

1. Audit `MappingFieldsPage` và modal tạo rule để xác định các điểm đang nói theo model cũ.
2. Thêm helper shadow namespace cho page này.
3. Đổi title, info card, action copy, preview copy sang semantics `Mapping Rules`.
4. Ghi rõ constraint backend legacy thay vì che giấu.
5. Build thật và ghi validation.
