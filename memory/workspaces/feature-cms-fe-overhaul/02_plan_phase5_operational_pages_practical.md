# Plan — Phase 5 Operational Pages Practical

1. Audit `ActivityManager` và `DataIntegrity` để xác định các điểm đang chỉ là "target_table shell".
2. Enrich `ActivityManager` bằng metadata từ `/api/registry`.
3. Enrich `DataIntegrity` bằng metadata từ recon report hiện có.
4. Đổi copy/operator guidance sang scope source/shadow thật.
5. Build thật và ghi lại phần nào còn bị chặn bởi backend legacy.
