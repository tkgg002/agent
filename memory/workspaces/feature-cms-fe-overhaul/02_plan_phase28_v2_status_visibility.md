# Phase 28 Plan - V2 Status Visibility

1. Audit read model hiện tại của `GET /api/v1/source-objects`.
2. Bổ sung `shadow_binding_id`, `bridge_status`, `metadata_status`.
3. Update types FE.
4. Update `TableRegistry` để render status badges mới.
5. Verify backend tests + frontend build.
