# 05_progress_fix_recon_istimestamptz.md: Audit Log Tiến độ

- [2026-08-26T14:55:00+07:00] [Agent:Gemini-3.6-Flash] Khởi tạo workspace `FixReconIsColTimestamptzSchema20260826`. Tracing nguyên nhân gốc rễ lỗi báo giả "Thừa ở Shadow": `IsColTimestamptz` bị xịt do schema lookup `"public"` khi truyền bare table name $\rightarrow$ Fallback `isTZ = false` $\rightarrow$ Trôi 7 tiếng (+7h) $\rightarrow$ Báo giả "Thừa ở Shadow". Lập giải pháp kỹ thuật `09_tasks_solution_fix_recon_istimestamptz.md` trình User phê duyệt.
- [2026-08-26T15:00:00+07:00] [Agent:Gemini-3.6-Flash] Chuẩn hoá kiến trúc theo đúng nhắc nhở của User: Không tự lookup schema bên trong `IsColTimestamptz` (tránh rủi ro trùng tên bảng giữa các schema). Phân giải `entry.ShadowSchema` ngay từ đầu ở caller `ChunkStreamBucketEngine.Execute` để `entry.QualifiedTarget()` luôn mang tên đầy đủ `schema.table`. Trình Demo Code mới cho User.
- [2026-08-26T15:35:00+07:00] [Agent:Gemini-3.6-Flash] Tiếp nhận phản hồi từ User với Payload thực tế (`POST /api/reconciliation/check`). Hoàn tác các đoạn code gượng ép trong `Execute`. Khắc phục tận gốc 2 điểm: (1) Sửa `QualifiedTarget()` tránh nhân đôi schema name. (2) Bổ sung Fallback Query xuyên user schemas cho `IsColTimestamptz` và `ColumnExists` trong `recon_dest_query.go`. Run `go test` PASS 100%.
- [2026-08-26T15:55:00+07:00] [Agent:Gemini-3.6-Flash] Tối giản mã nguồn theo chỉ đạo từ User: Loại bỏ 100% các hàm helper khử trùng lặp và fallback query thừa thãi. Trong `ReconJobWorker.HandleJobEvent` (`recon_job_worker.go`), gán trực tiếp `event.ShadowSchema` và `event.ShadowTable` từ Payload NATS event vào `entry`. Giữ nguyên 100% logic mã nguồn gốc.
- [2026-08-26T16:47:00+07:00] [Agent:Gemini-3.6-Flash] Truy vết và khắc phục tận gốc nguyên nhân theo phát hiện của User: (1) Sửa `pkgs/utils/type_inference.go` trong `cdc-cms-service` để `InferDataType` suy luận kiểu dữ liệu timestamp trả về `TIMESTAMPTZ` (thay vì `TIMESTAMP`). (2) Thực thi ALTER COLUMN đổi `updatedAt` và `createdAt` trong `shadow_traitestctphs.trans_his` sang `TIMESTAMPTZ` khớp 100% với `mapping_rule_v2`. Build `cdc-cms-service` và `centralized-data-service` PASS 100%.
