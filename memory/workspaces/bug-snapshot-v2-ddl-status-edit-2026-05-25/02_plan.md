# Tech Design & Implementation Plan (Song ngữ EN/VI)

## I. English Version
- Add a Radio.Group to the `content` of `Modal.confirm` inside `handleSnapshot` in `TableRegistry.tsx`.
- Dynamically pass `overwrite` flag to the snapshot API.

## II. Phiên bản Tiếng Việt
- Bổ sung Radio.Group vào `content` của `Modal.confirm` trong `handleSnapshot` ở file `TableRegistry.tsx`.
- Truyền động cờ `overwrite` vào API snapshot.
