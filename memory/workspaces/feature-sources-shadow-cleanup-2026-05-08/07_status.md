# Status

- Current phase: implemented and browser-verified.
- FE status: done for requested `/sources` UX cleanup, Flow1 removal, and `/shadow` rename.
- CMS status: done for connector config update API and fingerprint refresh path.
- Remaining gaps: no real end-to-end create against live MySQL/PostgreSQL source databases was executed in this session; UI/config generation path was verified in browser.
