# 05_progress.md — Reconcile Overhaul (Append Only)

- `[2026-06-10] [Muscle:Claude-Opus-4.8] Khởi tạo workspace. Đọc lessons (L459 heal-full-set, L942 silent-skip, L951/1875 timestamp_field registry-first, L806 post-publish probe) + 2 subagent khảo sát worker/cms/FE recon + verify DB thật: recon_runs=0, report=0, schedule disabled, trigger NATS recon-check ALL → activity "success" tables_checked=0 (false-positive). Root cause 3 lớp: synthesize thiếu ShadowSchema; CheckAll gate GetSchema("public"@5433) nil → skip hết; DestAgent nhận db 5433 + query không schema-qualify. Plan: 02_plan_recon_shadow_plane.md.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] Edit 1: model/table_registry.go +field ShadowSchema (gorm:"-", synthetic từ shadow_binding) — mục đích: entry registry mang schema shadow plane cho recon.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] Edit 2-7 (recon shadow-plane-aware): model/table_registry.go +QualifiedTarget(); metadata_registry_service.go synthesize +ShadowSchema; recon_dest_agent.go +quoteRelation() & thay quoteIdent(tableName)→quoteRelation (7 chỗ table, pkColumn giữ nguyên); recon_core.go gate GetSchemaInSchema(shadowSchema|public) + 6 call sites destAgent dùng QualifiedTarget() + đếm skipped_no_schema + Warn khi 0 checked (visibility); worker_server.go destAgent nhận shadowDB×2 + reconSchemaAdapter=NewSchemaAdapter(shadowDB) riêng (KHÔNG đụng adapter ingest — cô lập source→shadow); recon_handler.go CheckAll 0 tables → activity status "warning"; FE DataIntegrity.tsx fix invalidate key orphan recon-status→recon-report. NGOÀI SCOPE nhưng buộc sửa để verify: scan_array_path_test.go (PRE-EXISTING compile error — test cũ 2-return vs hàm 3-return, KHÔNG trong diff trước đó của tôi) sửa 2 dòng got,_,err.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] Verify: go build PASS, go vet PASS (service/handler/server/model), go test handler+service+transmute PASS, FE build PASS (443ms). Restart worker: kill PID 93684 (go run cũ), chờ port 8082 free (lesson L1918), chạy /tmp/cdc-worker-recon PID 9699 — log xác nhận shadow_plane 5436 connected + ReconCore initialized. Trigger NATS recon-check ALL → đang chờ recon_runs xuất hiện (background watch).`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] E2E PASS: recon_runs 4 rows success (672 windows/bảng); report: export_jobs DRIFT (src=20,dest=0,stale=1), export_jobs_test drift, wallet_capsets_1/2 ok → recon lần đầu chạy thật trên shadow plane + phát hiện drift thật. Phát hiện phụ: connection default_master (PG) thiếu DSN → recon PG source bị skip có WARN; schedule reconcile vẫn is_enabled=false (chờ Boss bật). Report: report_reconcile_overhaul_phase1_2026-06-10.md (git diff: worker 7 files +86/−22; FE 1 file +3/−1). Gap phase sau: heal per-source + G10 tsField + G1/G3/G5/G8 + FE GAP2/3/4/8.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] USER SỬA LƯNG: "kêu làm giải pháp tổng thể, mày chui vào code fix" — SAI DELIVERABLE (tactical thay vì strategic). DỪNG theo Rule 7 → ghi lesson nhóm 1 (deliverable-mismatch, 63→64 pattern) TRƯỚC → làm lại đúng đề: DESIGN tổng thể Reconcile V4 End-to-End. Verify nền tảng read-only: master 5434 + shadow 5436 đều có _gpay_id+_source_ts (join key Segment B); transmuter conflict=_gpay_id OCC=_source_ts (đã bỏ _hash khỏi master). Deliverables: 10_gap_analysis_recon_v4.md (gap matrix vs 4 trụ chuẩn; GIỮ engine 3-tier+lock, BỎ heal bypass, XÂY Segment B/alert/lag) + 09_tasks_solution_recon_v4.md (kiến trúc 2-segment E2E, watermark adaptive, heal re-trigger qua dbz-signal & transmute SourceIDs — từ chối dummy-update vào source, alert+recon_lag, code demo SQL/Go, roadmap P1-P4 ~7d) + report_recon_v4_design. 0 SOURCE FILE đổi turn này (git status = 6 file cũ turn trước). Services verify: 8082+8083 LISTEN. Chờ verb: approve recon v4 | revise.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] BOSS APPROVE recon v4 → thực thi P1 Segment B (shadow↔master). Edits: migration NEW cms 081_recon_segment_b.sql (ALTER report +segment +index, idempotent); model reconciliation_report.go +Segment; recon_core.go +block Segment B ~233 dòng (MasterBindingRef, listActiveMasterBindings, RunSegmentB — HashWindow 2 phía theo _gpay_id + drill-down diffIDs cap 50k + window-err KHÔNG nuốt, RunSegmentBFor, CheckAllSegmentB leader-gated, field masterAgent) — TÁI DÙNG HashWindow/ListIDsInWindow/locks có sẵn, không agent mới; worker_server.go wire masterDB (RoleDestination)→SetMasterAgent (warn+fix_hint khi thiếu); recon_handler.go payload +segment → handleReconCheckSegmentB (0-checked=warning). Verify: build PASS, test service+handler PASS (vet warn pkgs/idgen pre-existing ngoài diff); migration 081 applied 5433 (cột segment OK); worker restart PID 30499 masterAgent wired. E2E: NATS segment=shadow_master table=* → 6/6 bindings, 672 windows/binding; DRIFT TRANSMUTE THẬT: b3 missing 8, export_jobs_mt missing 2 (missing_ids đích danh [57409138828771376,58019585579810876] — cross-check tay: shadow_has=1, master_has=0 ✅) + orphan lớn export_jobs_mt(161)/mt_02(162) = dấu hiệu shadow re-snapshot sau materialise (input cho P2 heal chính sách ngưỡng). recon_runs tier=4 6 rows success. Report: report_recon_v4_p1_segment_b_2026-06-10.md. Git: worker 8 files +376/−28 (lũy kế nhánh recon), cms +1 migration. DoD P1 ĐẠT bằng drift thật.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] P2 Self-healing re-trigger. NEW internal/handler/recon_heal_v4.go (324 dòng): healSegmentB (report B → map _gpay_id→_source_id chunk 1000 trên shadowDB → publish cdc.cmd.transmute _source_ids; orphan KHÔNG tự xoá), healSegmentA (tier-2 missing IDs → publish cdc.cmd.debezium-signal incremental theo PK chunk; filter SQL IN / Mongo $in; KHÔNG dummy-update source), healThresholdBlocked (>5000 ID hoặc >5% → block + ERROR + activity blocked_threshold; small-table floor ≤100 ID — thêm sau khi test thật b3 9 ID bị block oan vì drift 63%), WithShadowDB + WithNatsPublisher (độc lập WithBackfill — fix trap natsPub nil khi backfill disabled trên V2). recon_handler.go: +shadowDB field, HandleReconHeal route segment + legacy escape-hatch. worker_server.go: wire shadowDB + natsPub. Verify: build/test PASS; restart worker. E2E DoD: (1) heal-B b3 drift thật → dispatched 8 → transmute inserted=7 updated=1 → RE-CHECK B: ok 11=11 missing 0, healed_count=8 — VÒNG KÍN ĐÓNG; (2) export_jobs_mt mismatch 165 drift 8050% → blocked_threshold ✅; (3) heal-A flow đúng (auto tier-2 → noop); ⚠️ heal-A signal row-level PENDING VERIFY (không Kafka Connect local) — ghi trung thực. Report: report_recon_v4_p2_self_healing_2026-06-10.md. Git: +1 file mới 324 LOC, recon_handler/worker_server sửa nhỏ.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] P3 Watermark adaptive + Lag monitoring. Worker: recon_core.go +adaptiveFreeze(clamp 5m..60m theo lag thực) + lagBetween (zero/skew→0 không phạt) + upsertReconLag (whitelist col) + pickScanRange đo ingest_lag (srcMax−shadowMax, tận dụng query sẵn) + RunSegmentB đo transmute_lag (shadowMax−masterMax) — cả 2 dùng adaptive freeze; NEW recon_lag_test.go (9 case unit gồm clamp + negative-path). CMS: NEW migration 082 recon_lag; recon_read_repo_gorm.go JOIN recon_lag + FIX DISTINCT ON (target_table, segment) — row B không che row A; read model +3 lag fields; model +Segment. Verify: build 2 service PASS, unit PASS, mig 082 applied, worker P3 restart (PID 47320) + trigger A+B → recon_lag 7 bảng SỐ THẬT (export_jobs_mt transmute_lag ~4.8 NGÀY đúng tình trạng lệch; b3 ~17.3h; khoẻ=0; export_jobs ingest=0), query per-segment+JOIN trả 12 rows đúng shape; CMS P3 restart (PID 47749) /health ok. Trung thực: worker_backlog NULL (Kafka lag glue=P4), FE render=P4, b3 lag 17h dù count khớp → row mới nhất master giữ bản cũ (OCC) — input cho P4 row-diff. Report: report_recon_v4_p3_watermark_lag_2026-06-10.md. Git: worker recon_core +63 P3 + test mới 50 LOC; cms 3 file +14/−4 + 1 migration.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] P4 (CUỐI) Alert + L3-B Row-diff + FE. ALERT: NEW recon_alert.go 87 LOC — FireAlert UPSERT thẳng cdc_system.cdc_alerts (fingerprint sha256 mirror byte-level cms persistence.Fingerprint, dedup occurrence++, không đè silenced) + alertOnReport hook sau Tier1 & RunSegmentB + ReconHealBlocked khi heal chặn ngưỡng → FE banner /api/alerts/active hiển thị sẵn. ROW-DIFF: dest_agent +IDTs/ListIDTsInWindow (+57); recon_core +diffIDTs (stale ĐÍCH DANH theo id+ts thay đếm window) + RunRowDiffB (re-derive expected từ _raw_data qua mapping rules approved — gjson+ApplyTransform, KHÔNG nhân đôi transmute; cap 200) + FieldDiff + SetPlaneDBs + deep param xuyên RunSegmentB/RunSegmentBFor/handler payload; migration 083 field_diffs JSONB + model 2 bên. FE: DataIntegrity cột Segment (tag A/B) + cột Lag (humanize m/h/d) + heal gửi segment (ReconRow types, useHealMutation, CMS ReconHealCommand +Segment forward). VERIFY E2E: build 3 service PASS, 94 test PASS, mig 083 OK; alert thật: ReconDrift{table,segment} + ReconHealBlocked firing trong cdc_alerts; row-diff: stale-inject userId=CORRUPTED trên master b3 → field_diffs bắt ĐÍCH DANH {column:userId, actual:CORRUPTED…, expected:61234…}; cleanup: revert → re-check ok 0/0, row xoá-ngoài-window phục hồi qua re-transmute. TRUNG THỰC: row-diff noise loose-compare với date/quoted (5/6 entries test) → cần type-aware normalize vòng sau; worker_backlog NULL (glue defer); sự cố start worker sai cwd (config relative) → phát hiện + start lại đúng. Git: worker 8 files +693/−34 +3 file mới; cms 5 files +27/−6 +mig 083; web +62. Report: report_recon_v4_p4_alert_rowdiff_fe_2026-06-10.md. RECON V4 ROADMAP P0-P4 HOÀN THÀNH.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] DASHBOARD Pipeline Grid (data-lineage, blueprint Boss). THUẦN FE 0 BE đổi: NEW src/components/ReconPipelineGrid.tsx 324 LOC — Tầng 1 Master Grid (mỗi dòng 1 pipeline Source→Shadow→Master, ghép Segment A+B theo lineage B.source_db↔A.target_table, cột counts 3 trạm + Drift 2 chặng âm-đỏ/dương-vàng + Recon cuối + Trạng thái Khớp/Lệch/Lagging/Cảnh báo) + Tầng 2 Drawer (flow map 3 trạm 2 chặng lag màu mức; convergence chart recharts theo phiên recon; nhật ký 30 phiên). useReconStatus.ts +useTableHistory (dùng endpoint TableHistory có sẵn — ĐÓNG GAP4) + type field_diffs (fix build error). DataIntegrity.tsx +tab Pipelines làm DEFAULT + import. Verify: npm run build PASS 781ms; data-shape đã verify P3 (12 rows per-segment+lag); services 3/3 LISTEN. Khác blueprint có chủ đích: counts theo window 7d (không full-count mỗi render), Auto-Heal% defer (có healed_count làm nguyên liệu), chart theo phiên recon không bịa time-series. Report: report_recon_dashboard_pipeline_grid_2026-06-10.md. Git web: +1 file mới 324 + useReconStatus +29 + DataIntegrity +14.`
- `[2026-06-10] [Muscle:Claude-Opus-4.8] FIX dashboard counts SAI (user report: 3 cột recs đang lấy window 7d, b3 hiện Master=11 trong khi bảng thật 464). Root cause: buildPipelines dùng A/B.source_count/dest_count = số TRONG WINDOW đối soát, blueprint cần TỔNG record tại thời điểm recon. Fix tận gốc: migration 084 report +total_source_count/+total_dest_count; worker RunTier1 đo totals (sourceAgent.CountDocuments Mongo full + destAgent.CountRows shadow — 2 hàm CÓ SẴN, lỗi đếm→NULL không fail run) + RunSegmentB đo (CountRows shadow + master exact); model 2 bên +2 fields; FE type + buildPipelines ưu tiên totals (fallback window cho report cũ) + driftAB/BC tính từ totals + sửa label drawer. Verify E2E: build 3 service PASS, mig 084 OK, worker restart, trigger A+B → report b3 totals 465/464 = ĐỐI CHIẾU TAY shadow 465 ✅ master 464 ✅ (lộ ngay 1 row thiếu thật ngoài window); export_jobs totals 168/170 = shadow tay 170 ✅ (drift +2 shadow giữ row source đã xoá). Files: mig 084 NEW, model×2, recon_core +2 đoạn, useReconStatus +2 fields, ReconPipelineGrid buildPipelines+label. Services 3/3 LISTEN.`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] FIX 3 issue dashboard (user): (1) CONNECTOR+MASTER_SCHEMA enrich read-side — listLatestPrimary +LATERAL mbm(master_binding.master_schema cho row B) + JOIN connection_registry cr qua so.source_connection_id (LẦN ĐẦU sai: tưởng shadow_binding có source_connection_code — FE type là enriched; verify SQL trên DB thật TRƯỚC khi build → sửa đúng đường so→cr); read model +SourceConnectionCode+MasterSchema; FE PipelineRow +sourceConnector (tag cyan dưới source) + masterName=FQN schema.table. (2) TIME SAI: checked_at=timestamp-WITHOUT-tz + worker ghi time.Now() local → lưu 17:32 khi UTC 10:33 → FE +7h nữa. Fix: 5 chỗ CheckedAt + 2 chỗ healed_at → .UTC(); DATA-FIX 1 lần: UPDATE 34 rows local-naive (checked_at>NOW-UTC) −7h — nếu không fix data, rows cũ "tương lai 7h" đè DISTINCT-latest suốt 7 tiếng. (3) COUNTS VẪN SAI: root = CMS chạy binary build TRƯỚC khi model có total_* → API không trả → FE fallback window; + đa số bảng chưa có report mới. Fix: rebuild+restart CMS (p4d có cr-join), trigger full A+B (phát hiện vận hành: B bị CheckAll-A giữ leader-lock TTL → phải chờ leader free mới chạy B). VERIFY: SQL per-segment latest — B 6/6 totals (aaa 170/170, b3 465/464, mt 170/163, mt_02 170/332, wallet 11101/11101) + UTC; A count_windowed có totals (456/465...). PHÁT HIỆN THÊM: rows check_type='count' totals-NULL đè latest = errorReport Tier1 SRC_TIMEOUT do schedule reconcile ĐƯỢC BẬT interval=1' (ai đó bật song song) → CheckAll chồng vòng nghẹt Mongo; chỉnh interval 1'→30' (đúng default thiết kế, config vận hành có ghi nhận). Worker restart p4e (gồm heal-UTC + code orphan-prune MỚI của bên khác trong working tree — build PASS chung). Files: recon_read_repo_gorm (3 edits), recon_read_models +2, recon_core 5×CheckedAt UTC, recon_heal_v4 2×now UTC, useReconStatus +2, ReconPipelineGrid +connector/FQN.`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] USER: "schedule reconcile nói chạy 09:56 mà chưa thấy chạy". VERIFY TRƯỚC: schedule THỰC RA ĐÃ kick đúng hẹn — recon_runs có run mới 02:58:57Z (=09:58+07), leader key đang heartbeat TTL 42s, log "executing scheduled operation reconcile". Root cause CẢM NHẬN SAI do UI: worker_server update last_run_at SAU khi op xong (vòng reconcile CheckAll stagger 5-10') → UI đóng băng "09:26" suốt lúc vòng 09:56 đang chạy → operator tưởng chết. FIX visibility tận gốc: chuyển update last_run_at/next_run_at/run_count lên ĐẦU vòng (mark-at-start; cadence chuẩn hơn: next=start+interval, không drift theo độ dài vòng như next=end+interval cũ). File: worker_server.go (di chuyển update block lên trước switch op).`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] VERIFY mark-at-start: build PASS, worker p4f (PID 85218); ép due (last_run −31') → tick kế last_run_at=03:11:30Z khi NOW=03:11:34Z (update 4s SAU KHI VÒNG BẮT ĐẦU) + leader EXISTS=1 + log cycle started → vòng đang chạy mà UI đã nhảy; run_count 8→9 tại start. Report: report_fix_schedule_lastrun_visibility_2026-06-11.md. Note: working tree có orphan-prune + usePruneMutation của bên khác — build chung PASS, không đụng.`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] FIX log spam SELECT COUNT(*) FROM "" (42601, recon_read_repo_gorm:330, ×6 mỗi poll 5s). ROOT CAUSE = LỖI CỦA TÔI: TIER COLLISION — backfill-source-ts đã chiếm tier=4 trong recon_runs (ListBackfillRuns lọc tier=4), tôi đặt Segment B tier=4 khi thiết kế KHÔNG grep consumer → run B (table_name FQN có dấu chấm) bị BackfillStatus nhặt nhầm → CountTableRows → PgIdent fail-close "" → query lỗi mỗi poll. FIX 3 TẦNG: (1) worker tierSegmentB 4→5 (tier 5 verified free); (2) cms ListBackfillRuns +guard instance_id LIKE 'backfill:%' (chặn vĩnh viễn nhặt nhầm); (3) CountTableRows guard ident=="" → error sớm không bắn query (+import fmt). DATA-FIX: recon_runs 25 rows + report 25 rows tier 4→5 (segment B cũ, phân biệt qua instance_id NOT LIKE backfill:). Verify: build 2 service PASS; restart worker p4g (PID 90730) + cms p4e; 20s polling log SẠCH (0×42601), endpoint backfill-status 200; trigger B b3 → recon_runs tier=5 ✅; 0 rows tier=4 non-backfill sót. Files: recon_core.go (const+comment), recon_read_repo_gorm.go (2 guards+fmt).`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] USER hỏi b3 thiếu 1 + "lag 17.4h là gì" → verify: 17.4h = max(_source_ts)shadow(02:29Z 10/6) − master(09:08Z 9/6); row thiếu đích danh _gpay_id=58019585403650053 (vào shadow sau lần transmute cuối; b3 KHÔNG có schedule sync → user xác nhận Manual, đã hiểu). EDGE-BUG lộ: window upper EXCLUSIVE = shadowMax → row mới nhất VĨNH VIỄN ngoài window khi idle (B báo ok 11=11 dù totals 465/464) → FIX +1ms cả RunSegmentB lẫn pickScanRange. Sau fix: window bắt ĐÍCH DANH missing=1 ["58019585403650053"] ✅.`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] CHUỖI FIX TIẾP theo dây chuyền user report: (1) Source=0 export_jobs → error 23505 recon_runs_one_running do 6 run mồ côi 'running' (worker kill giữa vòng) chặn recon vĩnh viễn → data-fix cancel 6 + ROOT-FIX beginRun tự hồi phục stale-running >15' (auto-cancel + retry 1 lần); verify đếm source THẬT qua remote cluster dev000 (10.200.187.x, db centra-3L-llized, coll export-jobs) = 168 → re-check A: totals 168/170 ✅ (ingest +2 orphan THẬT, không phải +170). (2) mt_02 master "332 không đúng" → đếm thật = 170: 332 là số ĐÚNG TẠI THỜI ĐIỂM ĐO 10:56Z (orphan-prune bên khác vừa dọn 162 row sau đó) — report B stale vì Segment B KHÔNG nằm trong scheduler → FIX: runReconcileCycle append CheckAllSegmentB (B tươi mỗi vòng 30'); re-check mt_02: ok 170/170 ✅. (3) b3 heal treo → transmute fail "ensure master destination: create index _source_id does not exist": EnsureMaster→Apply tạo UNIQUE INDEX theo spec.pk (flatten default _source_id) — cột đã bỏ khỏi master mới → FIX guard realCols (≠ seen vốn reserve tên đã bỏ — file vừa được bên khác sửa thêm cột _id) → hết 42703; heal giờ lộ tầng data: transmute degraded scanned=1 skipped=1 type_errors=1 — row bị TỪ CHỐI GHI ĐÚNG GATE (type validation, không NOT NULL rule nào — nằm sâu trong flatten transform của row test; b3=bảng Manual test, user đã nắm — deep-dive riêng nếu cần). Build/restart p4i→p4j; mt_02+export_jobs counts đúng trên grid. Files: recon_core.go (beginRun self-heal + 2×window +1ms), worker_server.go (+CheckAllSegmentB scheduler), master_ddl_generator.go (realCols guard spec.pk).`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] SOI SÂU row b3 58019585403650053 (user verb "soi sâu đi"). Truy 22P02: rules binding 10 đều nullable=t → loại NOT-NULL; log đích danh "master upsert failed _source_id=6a28cbecec5b9378333d594a: invalid input syntax for type json"; soi giá trị: _id=string, createdAt/lastUpdatedAt=number epoch, rule _id data_type=JSONB + cột master _id=jsonb → string trần vào JSONB = 22P02 (type_errors=1 = _id validate fail); coerceForColumn/sqlBind đã marshal đúng trên code MỚI — fail xảy ra trên binary cũ. GIỮA CHỪNG: worker bị bên khác restart (PID 17863, go run code working tree mới nhất — transmuter refactor "Cách 2 _id business col") → heal 14:01 qua worker mới: ROW VÀO MASTER (_id="6a28..." jsonb-quoted) → re-check B: OK 465/465 missing 0 — VÒNG KÍN ĐÓNG. Chuỗi nguyên nhân 4 tầng b3-thiếu-1: (1) Manual sync không ai transmute row mới; (2) window upper exclusive → recon mù (fixed +1ms); (3) EnsureMaster index spec.pk cột ma → heal chết 42703 (fixed realCols); (4) transmute 22P02 _id JSONB-string trên binary cũ (resolved bởi code transmuter mới của bên kia + coerce marshal). Dọn dẹp: ALTER SYSTEM log_min_error_statement bật tạm để bắt statement → ĐÃ RESET. Lưu ý realCols hardcode "_id":true giờ hơi lệch contract Cách-2 (_id chỉ real khi rule tạo) — rule target _id sẽ tự set qua loop nên vẫn an toàn cho case có rule; case spec.pk=_id không rule sẽ pass index nhầm — theo dõi (note, không sửa thêm giữa lúc bên kia đang refactor file này).`
- `[2026-06-11] [Muscle:Claude-Opus-4.8] USER: Source(recs)=0 tràn lan + activity error_count tăng 2→12. VERIFY: 9 bảng A latest = error 23505 recon_runs_one_running. Root MỚI (khác đợt trước): (a) interval bị chỉnh 30→10' trong khi 1 cycle 19 bảng mất 12-16' → VÒNG CHỒNG VÒNG; (b) bên khác dev restart worker liên tục (PID 17863→28402, instance_id đổi mỗi lần) → orphan running "TƯƠI" <15' → beginRun self-heal 15' không dám cancel → 23505 mỗi vòng → error rows đè latest → totals NULL → FE 0. FIX 3 TẦNG: (1) beginRun stale threshold 15'→5' + cancel theo instance_id<>mình (an toàn vì vào được beginRun = mình đang giữ advisory lock bảng đó = không ai chạy thật; lock chết theo connection khi process kill); (2) worker_server +reconCycleInFlight atomic.Bool — tick due khi cycle trước chưa xong → SKIP + Warn + activity (interval ngắn thành vô hại, không ép đổi config 10' của bên kia); (3) data-fix cancel orphan hiện tại. KHÔNG kill process dev của bên kia — code fix ăn từ lần restart kế của họ; orphan đã dọn nên Source hồi NGAY: verify export_jobs 168/170 ✅, users 323/326, testid1 456/456, wallet 11111 ✅; trigger 4 bảng còn error-row cũ (events, ej_2/_4/_test) — cùng cơ chế. Build PASS. Files: recon_core.go (beginRun 5'+instance), worker_server.go (+atomic guard +import).`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] BOSS: "recon quá lâu, 200-300 DBs, bảng 10-50tr, tính toán lại" → DESIGN-ONLY (lesson deliverable: verb strategic, KHÔNG nhảy vào code). ĐO THẬT 24h: Tier1-A avg 328.7s/bảng (max 2.4h) — thủ phạm = 1.344 round-trips/bảng (672 windows × 2) tới Mongo remote, KHÔNG phải data size; cycle 19 bảng ≈ 13'. CAPACITY MATH: 3K pipelines giữ kiến trúc cũ = 11.4 NGÀY/cycle (song song 16 vẫn 17h) — VỠ. Thiết kế V5 5 đòn bẩy: (1) bucket-aggregate server-side 672×2→2 queries/bảng (Mongo $dateTrunc $group + PG date_trunc GROUP BY + bit_xor PG15) → 328s→<2s; (2) totals O(1) estimatedDocumentCount + reltuples (hết collscan 50M/vòng); (3) bỏ stagger-sleep → pool 12 + semaphore 3/connection; (4) job-queue next_check_at + FOR UPDATE SKIP LOCKED multi-worker + adaptive backoff 15'→24h (bỏ leader-per-cycle); (5) incremental watermark last_verified_ts (scan delta thay 7d). KẾT QUẢ TÍNH: 3K pipelines = 9-13'/cycle 1 worker, 3-5' với 3 workers; hot-set hiệu dụng 2-3'. Roadmap V5-P1(2.5d)/P2(2d)/P3(1.5d) ≈ 6d, DoD đo bằng recon_runs.duration. Doc: 09_tasks_solution_recon_v5_scale.md. 0 code. Note: model report được bên kia thêm migration 085 (shadow_schema/shadow_table/run_id pipeline key). Chờ verb: approve recon v5 | revise.`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] BOSS: "thống kê các loại recon; action gì mà 1 bảng 300s; chỉ count 3 bảng thôi mà" → INVENTORY 10 loại recon (Tier1/2/3, SegB, prune, heal-A/B, row-diff, backfill, detector) + PHÂN RÃ 328s: 672 window-COUNT Mongo remote ≈ 135-170s + CountDocuments full collscan + khi mạng remote chập chờn (bench thật: connect timeout 30s — chính là nguồn run 2.4h). BOSS ĐÚNG: Tier1 trả lời câu đắt ("lệch ở khoảng nào") bằng 1.344 queries NGAY CẢ KHI KHÔNG LỆCH — thiết kế ngược. Cấu trúc lại V5 đơn giản hơn: TIER-0 count-only ~6 query O(1)/pipeline (estimatedDocumentCount + count/reltuples + MaxTs×3) ≈ 0.3-0.8s → 3K pipelines = 2-3'/cycle; bucket-aggregate demote thành Tier-1-CHỈ-KHI-LỆCH; +timeout 30s→5s + breaker per-connection skip-nhóm + alert ReconSourceDown (chống mạng remote). Doc: report_recon_actions_inventory_2026-06-12.md (gộp vào V5-P1). 0 code. Chờ approve.`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] BOSS "làm đi" → THỰC THI V5-P1. Edits: (1) recon_source_agent.go — getClient +serverSelectionTimeoutMS/connectTimeoutMS=5000 (fail-fast, chỉ client recon, không đụng pkgs chung) + NEW EstimatedCount (O(1) metadata) + NEW BucketCounts (1 aggregate $match+$group bucket-giờ, $toLong($toDate) chịu Date lẫn epoch-number, key=floor(ms/3600000)*3600000 khớp dest); (2) recon_dest_agent.go — NEW BucketStat + BucketCounts (1 query count+bit_xor(hashtextextended) per bucket); (3) recon_core.go — pickScanRangeWithLag (wrapper giữ chữ ký cũ cho Tier2); RunTier1 RESTRUCTURE: TIER-0 count-only (EstimatedCount+CountRows, tolerance max(1,0.1%), khớp→report ok check_type=count_total DỪNG) → TIER-1 bucket-aggregate CHỈ-KHI-LỆCH (2 query thay 1.344, fallback candidates chain, bucket khớp+totals lệch→1 CountDocuments exact phân xử estimate-noise vs drift-ngoài-window); CheckAll: BỎ stagger 5'+jitter → pool goroutine global-sem 8 + per-connection-sem 2 (thứ tự acquire cố định chống deadlock, mutex reports, giữ RunID 085 + per-table ctx 45s của bên kia); RunSegmentB: TIER-0 early-exit (totals khớp + transmuteLag=0 → ok DỪNG) → bucket count+xor 1 query/phía → drill-down ListIDTs CHỈ bucket lệch; totals tái dùng từ Tier-0 (hết đếm 2 lần); -import rand +sync. Build/vet/test PASS. BENCHMARK (điều kiện Mongo remote ĐANG DOWN): Segment B Tier-0 = 0.0-0.1s/bảng (từ 11.9s avg) ✅ 7/7 success; Segment A fail-fast 10s/bảng SONG SONG khi nguồn chết (trước treo 30-45s+ tuần tự, max 2.4h) — failed TRUNG THỰC; A happy-path (<1s DoD) PENDING VERIFY khi mạng remote hồi — ghi nhận, không chế số. Worker v5p1 PID 79453 RUNNING.`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] AUDIT V5-P1 sau ~5 vòng schedule TỰ ĐỘNG (data thật 2h): CYCLE WALL-TIME 60-63s (từ 13 PHÚT — 13×) trong điều kiện 12/19 bảng nguồn remote VẪN CHẾT; Segment B 35 runs avg 0.06s max 0.82s (từ 11.9s — 200×); A fail-fast avg 12.3s max 30s song song (từ treo 30-45s tuần tự/2.4h); cadence đều 10'/vòng (in-flight guard ổn). CÒN TỐN: khi nguồn chết, mỗi vòng đốt ~60s × 12 bảng fail vào mạng chết + 12 error rows/vòng (đúng quan sát Boss "tự chạy lại nhiều") → việc của V5-P2: connection-down skip-nhóm (1 probe/connection/vòng thay 12×) + adaptive backoff next_check_at (bảng fail liên tiếp giãn lịch) → cycle degraded dự kiến ~5s. A happy-path benchmark vẫn pending mạng (ping 3s timeout); watch nền đang chờ. Git nhánh chung worker: 13 files +1668/−170 (gồm cả orphan-prune/RunID 085 của bên kia — phần recon V4+V5 của tôi là chủ đạo recon_core/agents). 0 code đổi trong turn audit.`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] USER "source rec hoài không đúng" (Source=0 toàn grid trong khi mạng remote VẪN chết). ROOT: tag gorm default:0 trên SourceCount — errorReport để nil → gorm tự ghi 0 → error-row thành source_count=0 GIẢ (phá ý định NULL của migration 017 "phân biệt query-fail vs 0-thật") → FE hiện 0 + drift "ingest +N thừa" RÁC suốt lúc nguồn unreachable. FIX 3 lớp: (1) worker model bỏ ;default:0 khỏi SourceCount (nil = NULL thật); (2) FE buildPipelines guard aCountable = a.status!=='error' → sourceTotal/shadowTotal null → "—" + driftAB null (cả nhánh A+B lẫn A-only); (3) DATA-FIX 423 rows error source_count=0→NULL. Verify: build worker+FE PASS; latest A per bảng giờ source_count=NULL → grid hiện "—" thay 0; worker v5p1b PID 92469. Hành vi mới khi nguồn chết: Source "—" + trạng thái Cảnh báo (trung thực "không đếm được" thay vì số 0 bịa); mạng hồi → vòng kế tự điền số thật (watch nền vẫn trực A happy-path benchmark).`
- `[2026-06-12] [Muscle:Claude-Opus-4.8] MẠNG REMOTE HỒI — watch nền báo số A HAPPY-PATH V5 (vòng schedule TỰ ĐỘNG, không dàn dựng): 9/9 bảng SUCCESS avg 0.30s max 0.98s (events 188K rows=0.98s, wallet_capsets 11K=0.10s, export_jobs=0.57s) — DoD "<1s/bảng không drift" ĐẠT TUYỆT ĐỐI: 328.7s → 0.30s = ~1.100×. DoD V5-P1 chốt đủ 3: A 0.30s ✅, B 0.06s ✅, cycle 13'→60s-degraded/~giây-healthy ✅. Report P1 cập nhật trạng thái pending→done.`
