# Danh sách Tác vụ (Tasks) - SortTransmuteSchedulesLastRun20260826

- [ ] **Task 1**: Thêm `useMemo` sắp xếp mặc định `sortedData` theo `last_run_at` DESC trong `TransmuteSchedules.tsx`.
- [ ] **Task 2**: Thêm thuộc tính `sorter` và `defaultSortOrder: 'descend'` vào cột `Last run` trong `columns` của `TransmuteSchedules.tsx`.
- [ ] **Task 3**: Chạy `npm run build` để kiểm tra TypeScript compilation.
