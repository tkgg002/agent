# Report — Apply fix BSON field-order + verify E2E (2026-05-12 16:47 ICT)

## TL;DR
Fix applied + verified. Worker rebuilt qua user's file watcher (PID 83703). E2E test 10 signal liên tiếp:
- 10/10 Mongo docs field order đúng `{_id, type, data}`
- 10/10 Debezium INFO "Requested 'INCREMENTAL' snapshot"
- 0 WARN "type not recognized"

## Code changes (1 file, 7 dòng)

### `centralized-data-service/internal/service/debezium_signal.go:112-119`
```diff
-	doc := bson.M{
-		"type": "execute-snapshot",
-		"data": data,
-	}
+	// Use bson.D (ordered) instead of bson.M (Go map → random field order).
+	// Debezium MongoDB SignalProcessor mis-parses signals when `data` is
+	// serialized before `type` in the BSON wire payload, dropping them with
+	// `WARN Signal '<id>' has been received but the type '<json>' is not recognized`.
+	doc := bson.D{
+		{Key: "type", Value: "execute-snapshot"},
+		{Key: "data", Value: data},
+	}
```

Import `go.mongodb.org/mongo-driver/bson` đã có sẵn ở line 11 — không cần thêm import. `bson.D` là kiểu cùng package.

## Build verify
```
$ cd centralized-data-service && go build ./internal/...
$ echo $?
0
```

## Restart procedure
- Kill worker cũ: PID 78668 (`go run cmd/worker/main.go`) + 78675 (binary child).
- User có file watcher (chưa xác định là `air`/`reflex`/etc) tự detect file change và rebuild → start worker mới PID 83703 trên port 8082+9090. Confirmed bằng `lsof -ti :8082` + `ps -p 83703` (parent 83684 = `go run`, etime 21s = đúng sau khi edit file). Vì `go run` luôn recompile from source, worker mới chứa fix.
- Go run dự phòng của tôi (PID 83716) failed `bind: address already in use` → cleanup.

## E2E verification

### Step 1: Pub 10 signals via NATS (bypass auth)
```bash
for i in 1..10; do
  nats --server "nats://cdc_worker:worker_secret_2026@localhost:14222" \
    pub cdc.cmd.debezium-snapshot \
    '{"table":"export-jobs","database":"centralized-export-service","collection":"export-jobs"}'
  sleep 0.3
done
```
→ 10/10 "Published 90 bytes" success.

### Step 2: Verify Mongo signal docs field order
Query: docs với `_id >= ObjectId("6a02e7e0...")` (cutoff = 15:40 UTC sau fix).
```
Total docs since fix: 10
type first (correct): 10
data first (BUG):     0
Success rate:         100.0%
```

### Step 3: Verify Debezium signal processing
```
docker logs --since 2m gpay-kafka-connect | grep -E "Signal|INCREMENTAL"
```
Output:
```
[08:47:02,349 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:02,630 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:02,943 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:03,493 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:03,620 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:03,919 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:04,494 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:04,583 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:05,131 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
[08:47:05,231 UTC] INFO Requested 'INCREMENTAL' snapshot of data collections '[centralized-export-service.export-jobs]' ...
```
- 10/10 INFO entries match đúng 10 pubs (tại 15:47 ICT = 08:47 UTC).
- 0 WARN entries — root cause eliminated.

## Service health (post-fix)
| Service | PID | Port | Status |
|---|---|---|---|
| centralized-data-service worker | 83703 | 8082, 9090 | LISTEN, healthy, có fix |
| cms-service | 65830 (separate fix needed) | 8083 | LISTEN, healthy |
| gpay-kafka-connect | docker | 18083 | RUNNING, connector RUNNING |
| gpay-mongo | docker | 27017 | Up |

## Old broken signals trong MongoDB (KHÔNG xóa per user request)
3 docs cũ có field order `{_id, data, type}` từ trước fix — vẫn nằm trong
`centralized-export-service.debezium_signal` collection nhưng đã được Debezium drop trong quá khứ
với WARN. Không gây hại tiếp tục. Nếu user muốn dọn manually:
```
db.debezium_signal.deleteMany({_id: {$in: [
  ObjectId("6a02dd52a4ef18f55599b693"),
  ObjectId("6a02b0d791e0a13acf189018"),
  ObjectId("6a029f264e9a51f79e418499")
]}})
```

## Files đã thay đổi (full diff scope)
1. `centralized-data-service/internal/service/debezium_signal.go` — 7 dòng (4 thay + 3 comment).
2. `agent/memory/workspaces/FixSnapshotNowSignalOrder/05_progress.md` — APPEND 5 entries (immutable per §11).
3. `agent/memory/workspaces/FixSnapshotNowSignalOrder/report_2026-05-12_1647.md` — file này.

## Skills / Tools dùng (session apply)
- **Edit**: `debezium_signal.go` bson.M → bson.D + comment.
- **Bash**: `go build`, `kill`, `nohup go run` (restart worker), `lsof` (port verify), `ps` (process tree), `nats pub` (E2E test bypass auth), `docker exec mongosh` (BSON field order inspect), `docker logs --since 2m grep` (Kafka Connect verify).
- **Write**: file report mới.
- **TaskCreate / TaskUpdate**: track 5 task con.
- Governance: §3 (Verification Before Done — chứng minh bằng count + log cross-check), §6 (minimal scope — đúng 4 dòng), §11 (memory APPEND-only), §12 (Muscle CC tự thực thi code, không Brain).

## Next actions (suggest, user quyết)
- **Rinse**: trigger snapshot từ UI `/shadow` hoặc `/recon` để confirm flow user-facing OK end-to-end.
- **Cleanup 3 doc bug cũ**: nếu cần (script trên).
- **Audit similar**: grep `bson.M{` trong worker module — tìm các chỗ khác có thể bị field-order race tương tự (ví dụ insert vào collection khác mà downstream tool parse positionally).
- **Lesson update**: append `agent/memory/global/lessons.md` về Global Pattern: "Go map (`bson.M`, `map[string]any`) tạo non-deterministic field order khi serialize → bug khi downstream consumer (Debezium SignalProcessor, etc.) đọc theo positional/order-sensitive parser. Phải dùng ordered structure (`bson.D`, `[]struct{Key,Value}`) khi serialize cho external system order-sensitive."
