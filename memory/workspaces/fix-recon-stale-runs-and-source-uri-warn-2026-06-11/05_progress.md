# 05_progress (APPEND-ONLY — Audit Log)

| Thời điểm | Agent | Nội dung |
|---|---|---|
| 2026-06-11 ICT | Muscle:Claude-Opus-4.8 | **Khởi tạo workspace** + doc set (00_context, 01_requirements, 02_plan, 04_decisions, 08_tasks). Root-cause: (1) recon_runs treo do beginRun reactive-only (chỉ dọn khi run mới cùng table_name conflict) → bảng inactive treo vĩnh viễn; mỗi worker restart bỏ lại run 'running' instance cũ. (2) warn default_master do metadata RefreshCache resolve source-URI cho MỌI connection kể cả role=master 0 nguồn. Backup *.bak-2026-06-11: recon_core.go, worker_server.go, metadata_registry_service.go. |
| 2026-06-11 ICT | Muscle:Claude-Opus-4.8 | **Implement FIX 1+2**. recon_core.go: thêm `ReapStaleRuns(ctx)(int64,error)` — global cancel `status='running' AND started_at<NOW()-15min` → 'cancelled'+finished_at+error_message, log Warn. worker_server.go: gọi reaper (a) startup trong Start() trước Listen (guard reconCore!=nil), (b) đầu runReconcileCycle trước CheckAll. metadata_registry_service.go: build `referencedConnIDs` từ sources[i].SourceConnectionID; skip connection ∉ set trong loop resolve source-URI (hết warn default_master, giữ warn cho connection có nguồn lỗi thật). `go build ./...`=0. KHÔNG commit/push. |
| 2026-06-11 ICT | Muscle:Claude-Opus-4.8 | **Revise FIX 1 (D1-revised) sau quan sát realtime**: DB lúc verify có 5 run 'running' orphan **<15'** từ restart gần (instance `5a71a016` chết → `1db294c6`), row >15' cũ đã bị beginRun reactive cancel. Reaper age-based 15' có độ trễ → bug tái diễn mỗi restart. Thêm `ReconCore.ReapOrphanRunsFromDeadInstances(ctx)` (cancel `running AND instance_id IS DISTINCT FROM current`, dọn ngay orphan instance cũ + NULL legacy); startup đổi gọi method này (instance-based), giữ `ReapStaleRuns` age-based cho periodic. An toàn: leader-election 1 reconciler; xấu nhất cosmetic. |
| 2026-06-11 ICT | Muscle:Claude-Opus-4.8 | **Deploy + VERIFY end-to-end (GREEN)**. `go build ./...`=0; build `/tmp/cdc-worker-recon-p4h`. Phát hiện không có watcher/launchd — worker chạy binary detached do hệ thống build ngoài của user (p4..→p4j). Live worker p4j (build 11:55, code CŨ: 55 warn default_master + 72×23505). Thay p4j→p4h (rollback p4j còn disk). SIGKILL p4j để lại **3 orphan** → startup-reaper p4h `cancelled=3` NGAY (instance-based). Kết quả worker fix (p4h PID 17113, health 200, 18 NATS subjects, uptime ổn định): default_master warn=**0**, 23505=**0**, fatal/panic=**0**, running rows=**0|0**. Red→green chứng minh. Pre-existing ngoài scope: `schema_adapter.go:229 "id named in key"` (có sẵn log p4j/p4g). Bằng chứng: 06_validation.md. KHÔNG commit/push. |
