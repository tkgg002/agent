# 05_progress.md — DW Transform Patterns (Append Only)

| Timestamp | Agent | Action / Status |
|-----------|-------|-----------------|
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Workspace khởi tạo từ câu hỏi User (sync types/flatten/report/đối soát/Metabase). Chạy workflow `map-transmute-capabilities` (7 agent, 455k tok). |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Hoàn thành gap analysis (10_) + design phân tầng (02_). KL: chỉ `copy_1_to_1` có runtime; filter/groupby/aggregate/join/custom_sql = enum-only placeholder; master không có view/mart; Metabase vắng mặt. Chờ User chọn hướng Mart (A/B/C) + scope. |
| 2026-06-03 ICT | User | Reframe: ưu tiên PATTERN + cấu trúc source + system design TRƯỚC. Mỗi loại sync = 1 file plugin (copy file + add type + declare → chạy). Làm 2 type basic trước: 1:1 + flatten. Còn lại option sau. → 02_plan (mart/Metabase) hạ ưu tiên thành "option sau". |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Đọc full transmuter.go + transform_registry.go + child_explode.go. Thiết kế Strategy+Registry pattern (mirror transform_registry). Lưu 01_requirements_pattern + 09_solution_pattern. Dispatch point = processBatch (binding.TransformType:73 đang bị bỏ qua). |
| 2026-06-03 ICT | User | Chốt: sub-package transmute/ riêng + code ngay (framework + 1:1 + flatten). |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | IMPLEMENT XONG: package transmute/ (strategy/registry/copy_1_to_1/flatten/test/README) + refactor transmuter.go dispatch. build/vet/test PASS, full service suite PASS (no regression). Security gate: code mới CLEAN; fix Finding2 (separator ::idx::); flag Finding1 HIGH pre-existing child_explode.go:218 SQLi data_type→DDL (ngoài scope, đề xuất fix riêng). Chi tiết 03_implementation_pattern. |
| 2026-06-03 ICT | User | Redirect: quay lại masters-page-audit, KHÔNG bỏ audit còn lại. Nhấn mạnh 3 sync types (run_now/cron/post_ingest); realtime(post_ingest)=luồng CHÍNH; realtime cần lưu ý: (a) full sync action kiểu snapshot, (b) upstream incremental khi oplog Debezium/Kafka bắn qua. → audit realtime flow tiếp. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Audit realtime DONE (workflow 5-agent): incremental khi `_source_ids` non-empty (oplog→sink→cdc.cmd.transmute-shadow→fan-out→Run(ids)); FULL scan khi nil (cron/run-now=snapshot). transmuter.go:191-193 break sau batch đầu khi có ids. Gaps: NATS plain (no JetStream) post_ingest, incremental drop nếu >500 ids 1 batch. |
| 2026-06-03 ICT | User | Reframe flatten: KHÔNG auto-tạo. Phải scan→list field→duyệt→mới tạo cột master (kiểm soát schema master). |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Verify: flatten worker KHÔNG auto-DDL master (master_ddl_generator.go:69 chặn nếu chưa approved; transmuter chỉ nạp rule approved). Luồng scan→pending→approve ĐÃ CÓ (HandleScanRawData tạo mapping_rule_v2 status=pending). Lưu 09_solution_flatten_discovery. |
| 2026-06-03 ICT | User | CORRECTION (mid-session): đọc lessons/GEMINI trước; bỏ giọng option 1/2/3; rõ workspace nào; bắt buộc report_*.md (files+LOC); Simplicity First; verify build trước khi done. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | Đọc GEMINI.md + lessons.md, ghi lesson "multi-workspace sprawl + option-tone". KHẲNG ĐỊNH workspace active = feature-dw-transform-patterns-2026-06-03. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | TASK PLAN (User chốt): (A) Fix SQLi child_explode.go:218 validate data_type qua reTypeWhitelist. (B) Build scan-array: worker HandleScanArrayFields (cdc.cmd.scan-array) introspect element tại explode_path → pending mapping_rule_v2; CMS ScanArray endpoint + route. Mirror HandleScanRawData. → đang code. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | DONE (B) scan-array: worker HandleScanArrayFields + validScanIdent/explodePathToPGPath (injection-safe) + subscribe; CMS ScanArray + route POST /introspection/scan-array/:table; test PASS. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | DONE (A)+sweep SQLi: fix 4 site data_type→DDL bare (child_explode, HandleCreateDefaultColumns, master_ddl_generator CREATE+ALTER) bằng IsTypeWhitelisted dùng chung. Verify whitelist nhận NUMERIC(18,2) (no regression), chặn injection. |
| 2026-06-03 ICT | Muscle:Claude-Opus-4.8 | VERIFY: worker build=0, CMS build=0, vet=0 (trừ idgen pre-existing), test transmute 6/6 + handler scan_array PASS. Security gate 2 agent: code mới CLEAN, SQLi class fixed hết. Tạo report_transform_pattern_2026-06-03.md (files+LOC+verify). |
