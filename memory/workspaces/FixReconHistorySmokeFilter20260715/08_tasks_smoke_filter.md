# Danh sách Task - Lọc lịch sử đối soát theo smoke check

| STT | Task | Quyền thực thi | Trạng thái | Bằng chứng |
| --- | --- | --- | --- | --- |
| 1 | Sửa đổi logic `GetTableHistory` trong `recon_read_repo_gorm.go` để lọc `check_type` theo tham số `excludeSmoke`. | Muscle | TODO | |
| 2 | Chạy test backend (đối soát/history) để đảm bảo không bị lỗi biên dịch hoặc logic. | Muscle | TODO | |
