Bạn là Muscle (Chief Engineer). Nhiệm vụ của bạn là thực thi mã nguồn cho tính năng Snapshot V2 Control Plane.
Hãy đọc kỹ tài liệu /Users/trainguyen/Documents/work/agent/memory/workspaces/feature-snapshot-v2-control-plane-2026-05-25/09_solution.md và thực hiện các bước sau:

1. Tạo migration script (061 và 062) để thêm cột `total_rows` vào `snapshot_progress` và tạo bảng `snapshot_dlq`.
2. Cập nhật struct `SourceObjectRegistry` trong `internal/model/source_object_registry.go`.
3. Sửa hàm `buildUpsertSQLSnapshotInSchema` trong `internal/sinkworker/upsert.go` theo đúng logic LWW Guard Tie-Breaker.
4. Sửa hàm `runSnapshot` trong `internal/handler/snapshot_runner_handler.go` để tích hợp atomic.Bool, NATS Pause/Resume, MaxRPS Rate Limiting, EstimatedDocumentCount và gom batch DLQ (CreateInBatches).

Tuyệt đối tuân thủ Mandatory Directives trong tài liệu 09_solution.md. Sau khi làm xong, hãy chạy make test hoặc make build để đảm bảo không có lỗi biên dịch. Báo cáo lại kết quả khi hoàn thành.
