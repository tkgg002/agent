# Danh sách tác vụ: Tối ưu biểu đồ biến động số lượng phiên recon

- [x] Phân tích logic biểu đồ trong `ReconPipelineGrid.tsx` và tính toán miền trục Y (Y-Axis Domain) tối ưu.
- [x] Implement hàm tính toán `yDomain` động dựa trên min/max của `history.data`.
- [x] Cập nhật prop `domain={yDomain}` trên component `<YAxis>` trong `ReconPipelineGrid.tsx`.
- [x] Kiểm định build frontend (`npm run build`).
- [x] Cập nhật nhật ký tiến độ `05_progress_recon_chart.md` và báo cáo `11_report_recon_chart.md`.
