# Nhật ký Tiến độ (Audit Log): Concurrency & Batching Optimization

*Quy luật bất di bất dịch: Nhật ký tiến độ chỉ ghi nối tiếp (append-only), không chỉnh sửa lịch sử cũ.*

---

- [2026-07-08T22:15:00+07:00] [Agent:Gemini-1.5-Pro] **Khởi động Task:** Thiết kế giải pháp tối ưu hóa concurrency và batching cho Sink Worker và Transmute Worker. Đã tạo đầy đủ workspace `FixSinkTransmuteConcurrency20260708` và chuẩn bị các tài liệu requirements, plan.
- [2026-07-08T22:16:00+07:00] [Agent:Gemini-1.5-Pro] **Tạo Kế hoạch Triển khai:** Đã cập nhật `implementation_plan.md` làm artifact chính thức và đang chờ phê duyệt (approval) của User trước khi thực hiện viết code.
- [2026-07-08T22:20:00+07:00] [Agent:Gemini-1.5-Pro] **Đồng bộ Kế hoạch Triển khai:** Đồng bộ bản gốc của `implementation_plan.md` (dạng artifact) vào thư mục workspace `/Users/trainguyen/Documents/work/agent/memory/workspaces/FixSinkTransmuteConcurrency20260708/implementation_plan.md` để lưu giữ lịch sử kiểm toán của từng phiên.
- [2026-07-08T22:25:00+07:00] [Agent:Gemini-1.5-Pro] **Bổ sung Poison Pill & NATS Pull:** Nhận phản hồi từ User phê duyệt nâng cấp NATS Pull JetStream; đã bổ sung thiết kế chi tiết cơ chế cách ly Tin nhắn độc (Poison Pill Isolation) và cập nhật mã giả cho `DebounceBuffer` vào `12_implementation_plan_sink_transmute.md` cùng các tài liệu Full Doc Set (`00_context`, `03_implementation`, `04_decisions`, `06_test_cases`, `07_status_report`, `09_tasks_solution`, `10_gap_analysis`, `11_report`).
- [2026-07-08T23:05:00+07:00] [Agent:Gemini-1.5-Pro] **Bảo mật và Gia cố Kế hoạch (Red Teaming Hardening):** Nhận đóng góp sắc bén từ User về 4 lỗ hổng (Issue 1: Hủy chùm errgroup; Issue 2: Fallback mù khi sập DB; Issue 3: Trôi AckWait của NATS; Issue 4: Tràn RAM OOM). Đã cập nhật thiết kế chi tiết và mã giả của `DebounceBuffer` và `BatchBuffer` vào `implementation_plan.md` cùng `12_implementation_plan_sink_transmute.md` để "chống đạn" (bullet-proof) hoàn toàn.
- [2026-07-08T23:10:00+07:00] [Agent:Gemini-1.5-Pro] **Hoần tất Đặc tả Kế hoạch Tối ưu (12 Issues):** Xác nhận và viết lại toàn bộ kế hoạch triển khai dựa trên 12 điểm mù phân tán. Bổ sung thuật toán Binary Search Split $O(\log N)$ cô lập Poison Pill, Idle Timeout (Flush after idle), cơ chế Backpressure NATS Pull, Idempotent ON CONFLICT, và Event Ordering hashing. Đã đồng bộ đầy đủ các tệp workspace.
- [2026-07-08T23:20:00+07:00] [Agent:Gemini-1.5-Flash] **Bắt đầu Phiên Làm việc Mới:** Khôi phục context, xác minh trạng thái build thành công của worker (`go build ./cmd/worker`). Chuẩn bị lập kế hoạch triển khai chi tiết từng bước cho Muscle/Sub-agent thực thi các thay đổi code tối ưu hóa Concurrency & Batching CDC.
- [2026-07-09T00:00:05+07:00] [Agent:Gemini-1.5-Flash] **Tổng hợp Kế hoạch Triển khai:** Cập nhật và đồng bộ tệp `implementation_plan.md` ở thư mục gốc workspace để cung cấp một cái nhìn tổng thể toàn diện về cả tối ưu hóa backend CDC và các bản vá frontend của biểu đồ đối soát.
- [2026-07-09T00:02:00+07:00] [Agent:Gemini-1.5-Flash] **Khắc phục Sai lầm & Tạo Kế hoạch Mới:** Khôi phục tệp `implementation_plan.md` gốc về trạng thái ban đầu của workspace để không ghi đè/chỉnh sửa kế hoạch cũ. Đồng thời, khởi tạo tệp mới `12_implementation_plan_consolidated.md` chứa kế hoạch tổng hợp chi tiết theo đúng quy trình và yêu cầu của User.
- [2026-07-09T00:10:00+07:00] [Agent:Gemini-1.5-Flash] **Giải quyết 5 Vấn đề Nghiêm trọng:** Nhận phản hồi sâu sắc từ User về 5 vấn đề rủi ro (lỗi written nửa vời, term oan ID hợp lệ, transient error khi chia đôi, graceful shutdown thiếu wait, cạn connection do thiếu global semaphore). Đã cập nhật thiết kế chi tiết và mã nguồn Go hoàn chỉnh giải quyết các điểm nghẽn này vào tệp `12_implementation_plan_consolidated.md`.
- [2026-07-09T00:12:00+07:00] [Agent:Gemini-1.5-Flash] **Giải quyết 4 Lỗi Chí Mạng (Red Teaming Phase 2):** Tiếp thu 4 lỗ hổng mới phát hiện từ User (Sát thủ thầm lặng mất dữ liệu trong sub-batch, Deadlock do giữ Lock khi channel đầy ở triggerFlush, Orphaned Batch khi shutdown, và trôi AckWait khi chạy nhị phân đệ quy). Đã vá triệt để bằng cách thêm check `isTransientError` trong sub-batch, nhả Lock trước khi đẩy vào channel, xử lý đồng bộ mẻ cuối khi Done, và khởi chạy heartbeat goroutine định kỳ gửi `InProgress`. Đã cập nhật tệp `12_implementation_plan_consolidated.md`.
- [2026-07-09T00:15:00+07:00] [Agent:Gemini-1.5-Flash] **Gia Cố Thiết Kế Concurrency & Context (Red Teaming Phase 3):** Khắc phục triệt để 4 lỗi concurrency/context do User phản hồi (Lock Inversion giữa local/global semaphores, lỗi context cancel khi shutdown bị coi là poison pill, nghẽn channel triggerFlush khi shutdown, và InProgress rác trên tin nhắn đã xử lý). Đã đảo thứ tự local/global semaphores, thêm check context error vào `isTransientError`, bổ sung select ctx.Done khi push channel, tắt cảnh báo InProgress, và mô tả chi tiết giải pháp OpenTelemetry tracing cho đệ quy nhị phân. Đã đồng bộ đầy đủ các tệp workspace.
- [2026-07-09T00:18:00+07:00] [Agent:Gemini-1.5-Flash] **Khắc Phục 3 Lỗi Thiết Kế Runtime & Postgres (Red Teaming Phase 4):** Nhận phản hồi từ User về 3 lỗi mới (Heartbeat InProgress trên message đã hoàn tất gây panic, thiếu transient check cho lỗi serialization/deadlock của Postgres, và sai lệch lý thuyết semaphore). Đã vá triệt để bằng cách bọc trạng thái resolved dùng con trỏ atomic CAS vào `NatsMsgTask`, bổ sung lỗi Postgres 40001/40P01 vào `isTransientError`, đồng bộ đặc tả lý thuyết Local trước Global semaphore. Đã cập nhật tệp `12_implementation_plan_consolidated.md` và kiểm tra linter thành công.
- [2026-07-09T00:20:00+07:00] [Agent:Gemini-1.5-Flash] **Giải Quyết Lỗi Thất Thoát Dữ Liệu Khi Shutdown (Red Teaming Phase 5):** Nhận phản hồi về Vấn đề 19 (mất các batch đã đẩy vào `flushCh` nhưng chưa xử lý khi workerLoop tắt). Đã giải quyết bằng cách tích hợp vòng lặp non-blocking drain sạch kênh `flushCh` và gọi `Nak()` toàn bộ các tin nhắn đó để NATS phân phối lại sau khi khởi động lại. Đã cập nhật mã nguồn đặc tả trong `12_implementation_plan_consolidated.md` và hoàn thành audit linter.
