# report_2026-06-02.md — Snapshot.v2 mapping cache stale (REVISED)

## Bug
Source 66: approve mapping rule mới → snapshot.v2 không map field. Cache worker stale.

## Approach (Simplicity First — User redirect)
**Snapshot bypass cache, query `mapping_rule_v2` trực tiếp.** Snapshot là one-shot low-frequency, không cần cache. Realtime CDC vẫn dùng cache (high-throughput).

→ Không cần fix CMS publish, không cần fix cache invalidation, không cần NATS contract change.

## Patch
| File | LOC changed | Service |
|---|---|---|
| `centralized-data-service/internal/service/dynamic_mapper.go` | ~10 (thêm `MapDataWithRules`) | worker |
| `centralized-data-service/internal/handler/snapshot_runner_handler.go` | ~10 (query DB direct + bỏ ReloadAll + cursor loop) | worker |

**Tổng: 2 file, ~20 LOC, 1 service.**

## Approach cũ (BỎ — over-engineer)
Plan ban đầu sửa CMS `update_mapping_rule.go` (bỏ conditional publish) + worker post-reload count check + dispatch publish trước + Prometheus metrics + CI grep gate + integration test testcontainers. User feedback "quá lâu, chớt quớt" + "Simplicity First" → cắt xuống.

Files `02_plan.md` + `04_decisions.md` giữ làm forensics evidence, KHÔNG dùng cho execution.

## Workspace files
| File | Loại |
|---|---|
| 00_context.md | Bug context |
| 01_requirements.md | FR/NFR/AC (legacy plan) |
| 02_plan.md | Multi-phase plan (legacy, không dùng) |
| 03_implementation.md | **PATCH SPEC THỰC TẾ** (REVISED) |
| 04_decisions.md | ADR (legacy) |
| 05_progress.md | Append-only log |
| 06_validation.md | Smoke test (REVISED) |
| 07_status.md | READY_FOR_MUSCLE |
| 08_tasks.md | Task list (REVISED) |
| 09_tasks_solution.md | Muscle handoff (REVISED) |
| 10_gap_analysis.md | Forensics evidence 6 gap |
| report_2026-06-02.md | (file này) |

## Verify
1. Build + unit test pass.
2. Restart `centralized-data-service`.
3. Approve rule (không cần restart CMS).
4. Trigger snapshot.v2 source 66.
5. Log: `snapshot.mapping_rules.loaded count=N`.
6. Shadow table: column rule mới có mặt.
7. Regression: realtime CDC stream event ok (cache cũ).

## Rollback
`git revert` — 20 LOC, 1 service, an toàn tuyệt đối. Realtime CDC không bị động → 0 risk regression.

## Lesson candidate (sau User confirm)
> Global Pattern: [Path P chạy low-frequency one-shot dùng cache C shared với hot path H]. Cache stale → P sai. **Đúng**: P bypass C, fetch source-of-truth trực tiếp. Cache chỉ tồn tại cho hot path. Áp dụng: snapshot/replay/migration không cần cache; chỉ stream/realtime cần.
