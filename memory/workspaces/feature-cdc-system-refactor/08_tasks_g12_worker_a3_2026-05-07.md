# Tasks: G-12 Worker A3 Hybrid + Flow 1 P1 Smoke

**Workspace**: `feature-cdc-system-refactor`
**Date**: 2026-05-07
**Owner**: x2 (Antigravity gemini) — Muscle execute
**Brain**: max-Brain (Claude Opus 4.7) — plan/coordinate only
**Trigger**: Boss directive "bằng mọi giá phải lên đc flow1 này" + Brain peer (Antigravity) recheck convergence iter#73 (`report_flow1_recheck_brain_2026-05-07.md`)

---

## Definition of Done (overall)

1. Worker binary `/tmp/cdc-worker-host` chạy A3 hybrid build (mtime ≥ 2026-05-07).
2. Worker startup log có line `"PostgreSQL (shadow data plane) connected"` với `localhost:5436/cdc_shadow`.
3. ZERO line `SQLSTATE 42P01 relation does not exist` trong worker log sau swap (5 phút sustained).
4. ZERO line `SQLSTATE 42703 column "_id" does not exist` trong worker log sau swap (5 phút sustained — hoặc defer Mongo G-13 để query cast bigint không match).
5. `cdc_system.source_object_registry` 5 sources stuck `running` (id=11/26/29/30/44) advance ≥1 state OR fail với last_step_error rõ ràng (≠ blank).
6. Smoke Flow 1 PG: register fresh PG source → state machine reach `active` (or final state ≠ failed).
7. Verify Path B `shadow_<conn>.<table>` row count > 0 sau initial backfill (cho PG source mới).

---

## Checklist

### T-G12.1 — Stage worker A3 hybrid files (x2 thi công)

```bash
cd /Users/trainguyen/Documents/work/cdc-system/centralized-data-service
git add \
  internal/handler/provisioning_step_handlers.go \
  internal/handler/command_handler.go \
  internal/admin/source_register.go \
  internal/admin/helpers.go \
  internal/service/provisioning_orchestrator.go \
  internal/service/connection_manager.go \
  internal/server/worker_server.go \
  internal/sinkworker/sinkworker.go \
  pkgs/database/multi.go \
  config/config.go \
  internal/naming/ \
  docker-compose.yml \
  deployments/docker/Dockerfile.worker \
  deployments/sql/cdc/fix_g11_master_shadow_hyphen_2026-05-07.sql
```

**Verify**: `git status` → all 14 entries staged, no unstaged worker files left.

**DoD T-G12.1**: All 14 worker A3 files staged.

### T-G12.2 — Commit (x2 thi công, no Boss-gate)

```bash
git commit -m "feat(worker): A3 hybrid shadow DB routing + G-11 identifier normalize

- Add internal/naming package with NormalizeIdentifier helper (G-11 root cause fix)
- Inject ShadowDB *gorm.DB into ProvisioningStepHandler for Path B routing
- Swap schemaAdapter to shadowDB-backed adapter in HandleShadowBind
- Normalize source object names at struct literal in resolveShadowTarget
- Update connection_manager and multi-cluster pkg for hybrid Path A + Path B"
```

**Verify**: `git log --oneline -1` → new HEAD with subject `feat(worker): A3 hybrid...`

**DoD T-G12.2**: Commit landed, HEAD updated.

### T-G12.3 — Build worker binary (x2 thi công, no Boss-gate)

```bash
go build -o /tmp/cdc-worker-host.new ./cmd/worker
ls -la /tmp/cdc-worker-host.new
```

**Verify**:
- Build exit 0
- `/tmp/cdc-worker-host.new` mtime current (May 7 ≥ 16:00)
- File size > 50MB (current binary ~50.5MB)

**DoD T-G12.3**: New binary at `/tmp/cdc-worker-host.new`, executable, fresh mtime.

### T-G12.4 — Boss-gated swap PID 90006 (CHỜ Boss verb `swap worker`)

⚠️ **SHARED-SYSTEM MUTATION** — kill running worker. Cần Boss explicit approve trước khi chạy.

```bash
# Backup
cp /tmp/cdc-worker-host /tmp/cdc-worker-host.preG12.bak

# Stop old worker
kill -TERM 90006 && sleep 3

# Verify stopped
ps -p 90006 || echo "STOPPED"

# Swap binary
mv /tmp/cdc-worker-host.new /tmp/cdc-worker-host

# Start new worker
PROVISIONING_ORCHESTRATOR_ENABLED=1 nohup /tmp/cdc-worker-host > /tmp/cdc-worker-host.log 2>&1 &

# Wait + verify
sleep 5
curl -s http://127.0.0.1:8082/health
```

**Verify**:
- Old PID 90006 dead
- New worker process running, health 200 ok
- `grep -E "shadow data plane|cdc_shadow|5436" /tmp/cdc-worker-host.log` returns ≥1 line containing 5436 host:port

**DoD T-G12.4**: Worker swap LIVE, A3 hybrid log evidence visible.

### T-G12.5 — Verify state machine advance (x2 monitor)

```bash
# Snapshot stuck-source state immediately after swap
docker exec gpay-postgres-cdc psql -U gpay_admin -d cdc_dw -c "
SELECT id, object_code, provisioning_state, last_step_error
  FROM cdc_system.source_object_registry
  WHERE id IN (11, 26, 29, 30, 44)
  ORDER BY id;
"

# Wait 60s and re-check
sleep 60
docker exec gpay-postgres-cdc psql -U gpay_admin -d cdc_dw -c "
SELECT id, object_code, provisioning_state, last_step_error
  FROM cdc_system.source_object_registry
  WHERE id IN (11, 26, 29, 30, 44)
  ORDER BY id;
"

# Worker log scan for transmute errors
grep -E "SQLSTATE 42P01|SQLSTATE 42703" /tmp/cdc-worker-host.log | tail -10
```

**Verify**:
- ≥1 source advances state OR
- All 5 still `running` BUT no new `SQLSTATE 42P01` errors (worker successfully querying Path B; just waiting for triggers)

**DoD T-G12.5**: Worker no longer mis-routes; state machine functional.

### T-Smoke.1 — Register fresh PG source (x2 thi công)

```bash
TS=$(date +%s)
TABLE="orders_flow1_smoke_$TS"

# Create source table on goopay_source 5435
docker exec gpay-postgres-source psql -U src_user -d goopay_source -c "
CREATE TABLE IF NOT EXISTS public.$TABLE (
  id BIGSERIAL PRIMARY KEY,
  user_id INT,
  amount NUMERIC(10,2),
  status TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
INSERT INTO public.$TABLE (user_id, amount, status)
SELECT 1000+i, 100+i, 'pending' FROM generate_series(1,5) i;
"

# Register via cms admin API
curl -sS -X POST http://localhost:8083/api/sources/register \
  -H "Content-Type: application/json" \
  -d "{
    \"object_code\": \"flow1_pg_smoke_$TS\",
    \"source_connection_code\": \"postgres_primary\",
    \"source_object_name\": \"$TABLE\",
    \"source_object_type\": \"table\",
    \"primary_key_field\": \"id\",
    \"primary_key_type\": \"bigint\",
    \"timestamp_field\": \"created_at\",
    \"cdc_mode\": \"cdc\",
    \"sync_engine\": \"debezium\"
  }" | tee /tmp/flow1_smoke_register_$TS.json
```

**Verify**: Response 200 hoặc 201 với `id` mới và `provisioning_state="draft"` hoặc `"shadow_pending"`.

**DoD T-Smoke.1**: New source registered, ID returned.

### T-Smoke.2 — Drive state machine to `active`

```bash
# Monitor 5 phút (10 × 30s)
for i in {1..10}; do
  echo "=== iter $i ==="
  docker exec gpay-postgres-cdc psql -U gpay_admin -d cdc_dw -c "
    SELECT id, object_code, provisioning_state, last_step_error
      FROM cdc_system.source_object_registry
      WHERE object_code='flow1_pg_smoke_$TS';
  "
  sleep 30
done
```

**Verify**: Source progresses through:
`draft → shadow_pending → shadow_active → master_pending → master_active → discover_pending → discover_active → schedule_enable → active`

**DoD T-Smoke.2**: Source reaches `active` (or terminal state ≠ failed) within 5 phút.

### T-Smoke.3 — Verify Debezium + replication slot ACTIVE

```bash
# Publication
docker exec gpay-postgres-source psql -U src_user -d goopay_source -c "
  SELECT pubname, pubinsert, pubupdate, pubdelete
    FROM pg_publication
    WHERE pubname LIKE '%cdc%' OR pubname LIKE '%goopay%';
"

# Replication slot
docker exec gpay-postgres-source psql -U src_user -d goopay_source -c "
  SELECT slot_name, plugin, slot_type, active
    FROM pg_replication_slots
    WHERE slot_name LIKE '%cdc%' OR slot_name LIKE '%goopay%';
"

# Path B shadow row count
docker exec gpay-postgres-shadow psql -U gpay_admin -d cdc_shadow -c "
  SELECT count(*) FROM shadow_postgres_primary.$TABLE;
"
```

**Verify**:
- Publication exists với insert+update+delete enabled
- Replication slot exists, `active=true`
- Shadow table có ≥5 rows (initial 5 inserts đã sync)

**DoD T-Smoke.3**: CDC pipeline live, data flowing into Path B.

---

## Final Definition of Done (Flow 1 P1 GREEN)

- [x] T-G12.1 worker files staged
- [x] T-G12.2 commit landed
- [x] T-G12.3 binary built fresh
- [x] T-G12.4 swap LIVE (Boss-gated approve required)
- [x] T-G12.5 state machine functional, no SQLSTATE 42P01
- [x] T-Smoke.1 fresh PG source registered
- [x] T-Smoke.2 state machine reach `active`
- [x] T-Smoke.3 Debezium + replication slot ACTIVE, shadow table populated

---

## Constraints

- §0: Vietnamese
- §1: Brain (max) chỉ plan, KHÔNG sửa code
- §6: Simplicity First — minimal-impact patch (giữ nguyên scope T-G12)
- §7: APPEND-only memory; tạo task doc mới (08_tasks_g12), không edit cũ
- §8: Sau khi T-Smoke.3 GREEN, x2 RUN `/security-agent` per CLAUDE.md §8
- §11: TUYỆT ĐỐI không overwrite
- §12: Brain Code Prohibition — Boss-gated cho mọi shared mutation (T-G12.4)
- §14: Pre-flight check trước khi report DONE

---

## Boss verb dictionary (cần đúng 1 verb để mở task)

| Verb | Mở | Owner |
|---|---|---|
| `commit a3-worker` | T-G12.1 + T-G12.2 (no Boss-gate) | x2 |
| `build worker` | T-G12.3 (no Boss-gate) | x2 |
| `swap worker` | T-G12.4 (Boss-gated kill PID 90006) | x2 + Boss |
| `smoke flow1 pg` | T-Smoke.1 → T-Smoke.3 | x2 |
| `ship g12 full` | T-G12.1 → T-G12.5 (sequential, Boss approve T-G12.4) | x2 + Boss |
| `flow1 full chain` | T-G12.1 → T-Smoke.3 (full path, Boss approve T-G12.4) | x2 + Boss |

Brain (max) hold cho explicit verb. Heartbeat (/loop, "tiếp", "bằng mọi giá") không phải verb.

---

— max-Brain (loop iter#74 — formal task doc theo CLAUDE.md §7 Full Doc Set; pair với `09_tasks_solution_g12_worker_a3_2026-05-07.md` sẽ tạo nếu Boss yêu cầu solution doc riêng)
