# Tasks — Đợt J (cho x2 thi công)

> **Author**: max | **Recipient**: x2 | **2026-05-07 ICT**
> Plan chi tiết: `02_plan_dot_J_2026-05-07.md`. Coordination: `coordination_max_x2_2026-05-07.md`.

## J.1 Pre-flight check (5 phút)

- [ ] J.1.1 — `cd /Users/trainguyen/Documents/work/cdc-system/cdc-cms-service && git log --oneline -3` confirm HEAD = `b4a3461` (đợt I).
- [ ] J.1.2 — `go build ./... && go test ./... -count=1` baseline PASS.
- [ ] J.1.3 — Read body `internal/api/system_health_handler.go` để xác định ref pattern (`service.Snapshot`? `service.Collector`?).

## J.2 Decide destination dir

- [ ] J.2.1 — Pick **Option B** (`infra/observability/{,probes/}`) per max recommendation, hoặc justify alternative.
- [ ] J.2.2 — `mkdir -p internal/infra/observability/probes`.

## J.3 Bulk move (cluster A* + cluster C)

- [ ] J.3.1 — `cp` 7 file system_health_* (4 source + 3 test) → `internal/infra/observability/`.
- [ ] J.3.2 — `cp` 14 file probes/* → `internal/infra/observability/probes/`.
- [ ] J.3.3 — `sed -i '' 's/^package service$/package observability/'` 4 file system_health_* + 3 test (queries.go không có test).
- [ ] J.3.4 — Probes giữ `package probes` — không sed.
- [ ] J.3.5 — Sed import path internal: `cdc-cms-service/internal/service/health/probes` → `cdc-cms-service/internal/infra/observability/probes` ở `system_health_collector.go` line 28.

## J.4 Update callers cross-package

- [ ] J.4.1 — Sed `service.Collector/NewCollector/CollectorConfig` → `observability.X` ở `internal/server/server.go` (5 sites: L37, L235, L236, L262, L323).
- [ ] J.4.2 — Sed cùng patterns ở `internal/api/system_health_handler.go` (sites tùy J.1.3).
- [ ] J.4.3 — Edit (Edit tool — sed insert fragile cho multiline import) — thêm `"cdc-cms-service/internal/infra/observability"` vào import block, bỏ `"cdc-cms-service/internal/service"` nếu không còn ref.
- [ ] J.4.4 — (Optional cosmetic) Edit `internal/model/alert.go:12` comment: `service.AlertManager` → `persistence.AlertManager`.

## J.5 Drop service/ + verify empty

- [ ] J.5.1 — `rm internal/service/system_health_*.go`.
- [ ] J.5.2 — `rm -r internal/service/health`.
- [ ] J.5.3 — `ls internal/service/ 2>&1` → empty hoặc "No such file".
- [ ] J.5.4 — Nếu empty: `rmdir internal/service` (close Task #19 hoàn toàn).

## J.6 Verify build + test + DoD

- [ ] J.6.1 — `go build ./...` PASS, no output.
- [ ] J.6.2 — `go vet ./...` clean.
- [ ] J.6.3 — `go test ./... -count=1` PASS toàn repo (đặc biệt: api, infra/observability, infra/observability/probes, infra/persistence, infra/http, server).
- [ ] J.6.4 — DoD grep:
  ```bash
  grep -rEn "service\.(Collector|NewCollector|CollectorConfig|Snapshot|StatusOK|StatusDegraded|StatusDown|StatusUnknown|StatusUp)" --include="*.go" .
  ```
  Expected: 0 hit functional. Comment hits ok.
- [ ] J.6.5 — DoD grep import path:
  ```bash
  grep -rln "internal/service/health/probes" --include="*.go" .
  ```
  Expected: 0 hit.

## J.7 Commit cms code

- [ ] J.7.1 — `git status --short` review staged changes (22 rename + 2-3 modify).
- [ ] J.7.2 — `git add internal/service/ internal/infra/observability/ internal/server/server.go internal/api/system_health_handler.go internal/model/alert.go` (paths cụ thể, không `git add .`).
- [ ] J.7.3 — `git diff --cached --stat` confirm rename detection ≥98% (per cluster).
- [ ] J.7.4 — Commit theo template trong `02_plan_dot_J_2026-05-07.md` step 7. Subject: `refactor(cms): Task #19 đợt J — drain system_health_* + probes/ to infra/observability (Task #19 closed)`.

## J.8 Update workspace docs (agent repo)

- [ ] J.8.1 — APPEND vào `agent/memory/workspaces/feature-cdc-system-refactor/05_progress.md` entry mới:
  - Header: `[2026-05-07 ICT] Đợt J (x2) — Task #19 CLOSED — drain system_health_* + probes/ to infra/observability`
  - Author + commit hash + diff stat + DoD grep evidence + sub-list per file moved.
- [ ] J.8.2 — Update `coordination_max_x2_2026-05-07.md` "Task #19 closed at cms `<commit>`. Cms-lane unlock back to shared".
- [ ] J.8.3 — Commit agent docs:
  ```
  cd /Users/trainguyen/Documents/work/agent
  git add memory/workspaces/feature-cdc-system-refactor/05_progress.md \
          memory/workspaces/feature-cdc-system-refactor/coordination_max_x2_2026-05-07.md
  git commit -m "docs(workspace): Task #19 closed by x2 — đợt J landed"
  ```

## J.9 Optionally rebuild cms-server (Q3 final)

- [ ] J.9.1 — Coordinate với Boss: `kill PID 33841 && go build -o /tmp/cdc-cms-service-postJ ./cmd/server && /tmp/cdc-cms-service-postJ &`.
- [ ] J.9.2 — Smoke test `curl localhost:8083/api/health` (port theo config).
- [ ] J.9.3 — Verify cms-server log không panic, alert/approval/registry endpoints respond 200.

## J.10 Handover back to max (Brain)

- [ ] J.10.1 — Khi J.7+J.8 done: post completion msg vào coordination file hoặc 05_progress.md.
- [ ] J.10.2 — max sẽ resume worker-lane: fix sub-issues (duplicate close-loop log, P3 prune residue 1 row legacy_1) hoặc plan Track E (Mongo CDC).

---

## Acceptance criteria (Task #19 closure)

- ✅ `internal/service/` empty hoặc removed.
- ✅ All Bucket A → `infra/persistence/` (đợt G/H/I), Bucket A*/C → `infra/observability/{,probes/}` (đợt J).
- ✅ Build/test PASS toàn repo.
- ✅ DoD grep stale `service.<symbol>` = 0 hit functional.
- ✅ Atomic 2-commit landed: cms code + agent docs.
- ✅ Task #19 marked closed in workspace progress + active_plans.md.

---

## Notes for x2

- max KHÔNG đụng cms code. Mọi câu hỏi về cms code → x2 tự quyết hoặc hỏi Boss.
- Nếu pre-flight fail (build vỡ ở HEAD `b4a3461`) — STOP, ping max + Boss. Đợt J không kick off với baseline broken.
- Risk lớn nhất: `system_health_handler.go` body chưa đọc → step J.1.3 BẮT BUỘC. Skip = build vỡ ở step J.6.
- Pattern proven 4 đợt liên tiếp (G/H/I) — high confidence x2 close trong 30–45 phút.

— max
