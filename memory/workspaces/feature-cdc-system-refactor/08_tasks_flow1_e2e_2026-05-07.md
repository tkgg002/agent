# Tasks — Flow 1 E2E (cho x2 review + execute)

> **Author**: max | **Recipient**: x2 | **2026-05-07 ICT**
> **Read first**: `01_requirements_flow1_e2e_2026-05-07.md` + `02_plan_flow1_e2e_2026-05-07.md`
> **Output expected from x2**: `09_tasks_solution_flow1_x2_2026-05-07.md` với cmd-level details

---

## P1 — Smoke ngay (max ops, no code)

- [ ] P1.1 — Tìm `<ADMIN_API_PORT>` + `<ADMIN_TOKEN>` từ `centralized-data-service/config/config-local.yml` hoặc env. (x2 lookup hộ nếu thuận tiện vì cms-lane có thể có copy.)
- [ ] P1.2 — Tạo source table `public.orders_flow1_smoke_<TS>` ở PG source (5435) với 5 rows.
- [ ] P1.3 — POST `/v2/sources/register` cho `flow1_smoke_pg_<TS>`. Save response → `/tmp/flow1_register_response.json`.
- [ ] P1.4 — Verify AC-1: HTTP 200, `provisioning_state='active'`, `steps_completed` chứa 4 step.
- [ ] P1.5 — Verify AC-2: `curl :18083/connectors/cdc-pg-source/status` → state RUNNING + tasks RUNNING.
- [ ] P1.6 — Verify AC-3: `psql 5433 \dn` shadow_goopay_source schema tồn tại (đã có sẵn).
- [ ] P1.7 — Verify AC-4: `psql 5433 \d shadow_goopay_source.orders_flow1_smoke_<TS>` có pk + 8 CDC + business cols.
- [ ] P1.8 — Verify AC-5: `shadow_binding.ddl_status='created'` (NOT pending).
- [ ] P1.9 — Verify AC-6: `source_object_registry.provisioning_state='shadow_active'`.
- [ ] P1.10 — Verify AC-7: kafka-console-consumer topic `cdc.gpay.public.orders_flow1_smoke_<TS>` ≥1 msg (sau snapshot).
- [ ] P1.11 — Trigger snapshot via `dbz_signal` insert.
- [ ] P1.12 — Wait 30–60s. Verify AC-8: `count(*) FROM shadow_goopay_source.orders_flow1_smoke_<TS>` ≥ 5.
- [ ] P1.13 — Tạo `report_flow1_smoke_p1_2026-05-07.md` với raw output từng bước + pass/fail mỗi AC.

**Decision gate**: Nếu P1 8/8 PASS → skip P2 (root cause stuck pending là legacy data, không phải Flow 1 bug). Tiếp Phase 3 hardening. Nếu fail bất kỳ AC nào → P2 root-cause.

## P2 — Fix root cause stuck pending (chỉ chạy nếu P1 fail hoặc Boss yêu cầu)

- [ ] P2.1 — Read full `centralized-data-service/internal/service/provisioning_orchestrator.go` (~236 line).
- [ ] P2.2 — Grep `provisioning_state\s*=\s*'active'` toàn worker repo. Liệt kê sites set state direct (bypass state machine).
- [ ] P2.3 — Verify migration 047 backfill: `grep -A 20 "047_" centralized-data-service/migrations/cdc/` xem state mapping.
- [ ] P2.4 — Check NATS publish error handling tại `provisioning_orchestrator.go:236` — có persist `last_step_error` không?
- [ ] P2.5 — Determine fix approach:
  - Fix A (idempotent SQL re-fire one-shot)
  - Fix B (orchestrator code wrap publish + retry + log error)
  - Fix C (CLI tool `cmd/refire_stuck_bind/main.go`)
- [ ] P2.6 — Implement fix (worker-lane → max). Build PASS + test PASS.
- [ ] P2.7 — Re-fire 4 stuck rows: src_id 33,34,35,37. Verify `ddl_status='pending' → 'created'` trong 60s.
- [ ] P2.8 — Verify `count(*) FROM information_schema.tables WHERE table_schema LIKE 'shadow_%'` tăng theo (table thực sự CREATE).

## P3 — Hardening

### P3.1 — Add `POST /api/v1/sources/test` (cms-lane → x2)

- [ ] P3.1.1 — Plan endpoint signature theo `02_plan §3.1`. Body schema: `{engine_type:'postgresql'|'mongodb'|'mariadb', locator:{database, schema?, table_or_collection}}`.
- [ ] P3.1.2 — Implement `cdc-cms-service/internal/api/source_test_handler.go` — 3 engine support.
- [ ] P3.1.3 — Wire route `internal/router/router.go` POST `/api/v1/sources/test` (auth required).
- [ ] P3.1.4 — Add unit test `internal/api/source_test_handler_test.go`.
- [ ] P3.1.5 — Smoke: `curl POST /api/v1/sources/test` cho 1 PG source thực → response `{ok:true, schema_introspect:{cols:[id,...]}}`.
- [ ] P3.1.6 — Update FE wizard step 1.5 (mới) gọi endpoint test trước Step 2 register. **Skip nếu out of scope FE.**
- [ ] P3.1.7 — Commit subject `feat(cms): POST /api/v1/sources/test endpoint for Flow 1 Step 2 connection check`.

### P3.2 — PG/MariaDB preflight gate (worker-lane → max)

- [ ] P3.2.1 — Read `provisioning_step_handlers.go:137–142` Mongo preflight pattern.
- [ ] P3.2.2 — Implement `preflightPGSource(ctx, db, schema, table)` mirror Mongo: connect source PG → `SELECT count(*) FROM <schema>.<table> LIMIT 1` → refuse nếu table missing.
- [ ] P3.2.3 — Implement `preflightMariaSource(ctx, db, table)` similar — `SELECT 1 FROM <db>.<table> LIMIT 1`.
- [ ] P3.2.4 — Wire trong `HandleShadowBind` switch by engine type (mirror `isMongoEngine` pattern).
- [ ] P3.2.5 — Unit test cover 3 engine + missing-table case.
- [ ] P3.2.6 — Build PASS + test PASS toàn worker repo.
- [ ] P3.2.7 — Commit subject `feat(worker): extend Cascade Liability preflight gate to PG + MariaDB`.

### P3.3 — NATS publish promote-to-fatal (worker-lane → max)

- [ ] P3.3.1 — Read `internal/admin/source_register.go:87` (sub-step 4) — `nats.Publish("cdc.cmd.kafka.refresh-topics", ...)` non-fatal log.
- [ ] P3.3.2 — Implement 3-retry exponential (1s, 2s, 4s) with `time.Sleep` + final fatal return.
- [ ] P3.3.3 — Update HTTP response: 200 nếu pass, 500 nếu retry exhausted.
- [ ] P3.3.4 — Add `markProvisioningFailed(sourceID, "step4_failed", err)` nếu retry fail.
- [ ] P3.3.5 — Unit test cover happy + 3-retry-success + 3-retry-fail.
- [ ] P3.3.6 — Commit subject `fix(worker): promote NATS refresh-topics publish to 3-retry-then-fatal`.

## P4 — Fix MariaDB Debezium plugin (Boss-approved infra)

- [ ] P4.1 — Verify thiếu plugin: `docker exec gpay-kafka-connect ls /usr/share/confluent-hub-components/`.
- [ ] P4.2 — Locate kafka-connect image config (docker-compose.yml hoặc Dockerfile).
- [ ] P4.3 — Update image build / `confluent-hub install` để add `debezium-debezium-connector-mysql:2.5.4`.
- [ ] P4.4 — **Boss approve restart** kafka-connect container.
- [ ] P4.5 — `docker restart gpay-kafka-connect`. Wait healthy.
- [ ] P4.6 — Verify connector `cdc-mariadb-source` self-recover RUNNING. Curl `:18083/connectors/cdc-mariadb-source/status`.
- [ ] P4.7 — Test MariaDB sync end-to-end: 1 row insert MariaDB → verify shadow PG receives.
- [ ] P4.8 — Commit infra change.

## P5 — Cleanup

### P5.1 — Re-run prune_legacy_v1_bindings.sql (max)

- [ ] P5.1.1 — Read `centralized-data-service/deployments/sql/cdc/prune_legacy_v1_bindings.sql` confirm idempotent.
- [ ] P5.1.2 — Run: `docker exec -i gpay-postgres-cdc psql -U gpay_admin -d cdc_dw < <path>`.
- [ ] P5.1.3 — Verify `SELECT count(*) FROM cdc_system.source_object_registry WHERE object_code LIKE 'legacy_%' AND is_active=true;` = **0**.
- [ ] P5.1.4 — Investigate root cause `legacy_1` còn active sau prune đầu — có thể `ESCAPE '\'` clause không match (postgres LIKE escape rules).

### P5.2 — Dedup duplicate close-loop log (max)

- [ ] P5.2.1 — Verify duplicate ratio: `tail -200 /tmp/cdc-worker-host.log | grep "schedule closed" | sort | uniq -c` — count per schedule_id per timestamp.
- [ ] P5.2.2 — Read `centralized-data-service/internal/service/job_monitor.go` HandleCompleted handler.
- [ ] P5.2.3 — Investigate: subscribe duplicate (boot + retry?) vs handler call multi.
- [ ] P5.2.4 — Fix: `sync.Once` ở subscribe boot point hoặc dedup-by-event-id.
- [ ] P5.2.5 — Build PASS + test PASS.
- [ ] P5.2.6 — Restart worker (Boss approve). Tail log 60s — verify dedup OK.
- [ ] P5.2.7 — Commit subject `fix(worker): dedup duplicate close-loop log in JobMonitor`.

---

## Acceptance criteria (Flow 1 E2E)

- ✅ P1 smoke 8/8 AC PASS within 5 phút
- ✅ P2 stuck pending count = 0 (4 → 0)
- ✅ P3.1 endpoint `/api/v1/sources/test` PASS smoke
- ✅ P3.2 preflight extended PG + MariaDB
- ✅ P3.3 NATS refresh non-fatal → 3-retry-fatal
- ✅ P4 MariaDB connector RUNNING (Boss approved)
- ✅ P5 cleanup pruned + dedup log

---

## Workflow gate (per Boss directive)

1. max output `01_req` + `02_plan` + `08_tasks` (this) ✅
2. **x2 review** + write `09_tasks_solution_flow1_x2_2026-05-07.md` với:
   - Acknowledge / counter-propose
   - Cmd-level details cho từng task (exact curl + exact file:line + exact SQL)
   - Estimate effort per task
3. **Boss approve** `09_tasks_solution_*` đặc biệt cho Phase 4 (kafka-connect rebuild + restart)
4. **Execute**:
   - x2: P3.1 (cms)
   - max: P1, P2, P3.2, P3.3, P5
   - Boss: P4

## Notes for x2

- Lane lock ngày 2026-05-07 ICT effective: cms = x2, worker = max.
- max plan strategic, x2 plan tactical (cmd-level). Đừng implement trước khi `09_tasks_solution_*` được Boss approve.
- Boss directive: "bằng mọi giá phải lên đc flow1" → ưu tiên P1 smoke trước. Nếu PASS, harden có thể defer.
- Risk lớn nhất: P2 stuck pending có thể không liên quan tới Flow 1 happy-path (legacy data drift). P1 sẽ tell.

— max
