# Kế hoạch công việc (Tasks List)

- [x] Task 1: Bổ sung helper check `isShadowOff` và `isMasterSyncOff` trong `ReconPipelineGrid.tsx`.
- [x] Task 2: Áp dụng filter cho `pipelines` trước khi đưa vào `flatData` và cập nhật `loading` state của Table.
- [x] Task 3: Chạy `npx tsc --noEmit` hoặc `npm run build` trong `cdc-cms-web` để kiểm định cú pháp & build frontend.
- [x] Task 4: Kiểm tra lại các Gates G1-G8 và báo cáo kết quả.
